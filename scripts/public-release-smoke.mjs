import { chromium, webkit } from 'playwright';
import { mkdir,writeFile } from 'node:fs/promises';
import assert from 'node:assert/strict';
const root=new URL('../artifacts/',import.meta.url).pathname;await mkdir(root,{recursive:true});const results=[];
for(const [name,type] of [['chrome',chromium],['webkit',webkit]]){
 const browser=await type.launch({headless:true});const errors=[];const requests=[];const result={browser:name};
 try{
 const hostContext=await browser.newContext({viewport:{width:390,height:844},isMobile:true,hasTouch:true}),guestContext=await browser.newContext({viewport:{width:844,height:390},isMobile:true,hasTouch:true});const host=await hostContext.newPage(),guest=await guestContext.newPage();
 const observe=page=>{page.setDefaultTimeout(30000);page.on('pageerror',e=>errors.push(e.stack??e.message));page.on('response',r=>{if(r.status()>=400)requests.push({status:r.status(),path:new URL(r.url()).pathname});});};for(const page of [host,guest])observe(page);
 const release=await hostContext.request.get('https://andeplane.github.io/fuse-riders/release.json');result.release=await release.json();
 await host.goto('https://andeplane.github.io/fuse-riders/');await host.getByRole('button',{name:'CREATE ROOM',exact:true}).click();await host.waitForURL(/room=/);const invite=host.url();result.created=true;
 await host.getByPlaceholder('Your name').fill('Public host');await host.getByRole('button',{name:'JOIN AS PLAYER',exact:true}).click();await host.locator(':is(.online-roster,.room-riders):visible').getByText('Public host',{exact:false}).waitFor();
 await host.getByRole('button',{name:'ADD AI',exact:true}).click();await host.getByRole('button',{name:/Remove AI/}).waitFor();result.aiAdded=true;
 await guest.goto(invite);await guest.getByPlaceholder('Your name').fill('Public guest');await guest.getByRole('button',{name:'JOIN AS PLAYER',exact:true}).click();await host.locator(':is(.online-roster,.room-riders):visible').getByText('Public guest',{exact:false}).waitFor();result.guestJoined=true;
 await host.getByRole('button',{name:'START RACE',exact:true}).click();await guest.locator('.online-notice').filter({hasText:/READY/}).waitFor();result.countdownReceived=true;
 await host.waitForFunction(()=>!document.querySelector('.online-notice')?.textContent?.includes('READY'));await host.waitForFunction(()=>document.querySelector('canvas')?.dataset.renderer==='phaser-webgl');
 result.metrics=await guest.locator('#app').getAttribute('data-metrics');result.renderer=await host.locator('canvas').getAttribute('data-renderer');await host.screenshot({path:`${root}/public-phaser-${name}.png`});result.screenshot=`artifacts/public-phaser-${name}.png`;
 await host.locator(':is(.online-roster,.room-riders):visible').getByText(/ · [1-9] wins/).first().waitFor({timeout:25000});result.scoring=true;
 assert.ok(await host.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1),'phone host fits viewport');result.phoneHost={width:390,height:844,createdJoinedStarted:true};
 await host.getByRole('button',{name:'MAIN MENU',exact:true}).click();await guest.getByText('Join your friends, then start the race',{exact:true}).waitFor();result.reset=true;
 await host.getByRole('button',{name:'ROOM SETTINGS',exact:true}).click();await host.getByRole('radio',{name:'Shared TV + phone controls',exact:true}).check();await host.getByLabel('Match length').fill('2');await host.getByRole('button',{name:'CONFIGURE POWERUPS',exact:true}).click();await host.locator('dialog label').filter({hasText:'Blast radius'}).locator('input').fill('0');await host.getByRole('button',{name:'← BACK TO ROOM SETTINGS',exact:true}).click();await host.getByRole('button',{name:'SAVE SETTINGS',exact:true}).click();
 await guest.locator('.online-arena').waitFor({state:'hidden'});await host.locator('.online-arena').waitFor({state:'hidden'});result.sharedPhoneViewsHidden=true;
 await host.reload();await host.locator(':is(.online-roster,.room-riders):visible').getByText('Public host',{exact:false}).waitFor();await host.getByRole('button',{name:'ROOM SETTINGS',exact:true}).click();assert.equal(await host.getByRole('radio',{name:'Shared TV + phone controls',exact:true}).isChecked(),true);assert.equal(await host.getByLabel('Match length').inputValue(),'2');await host.getByRole('button',{name:'CONFIGURE POWERUPS',exact:true}).click();assert.equal(await host.locator('dialog label').filter({hasText:'Blast radius'}).locator('input').inputValue(),'0');
 result.savedPreferences=await host.evaluate(()=>{const s=JSON.parse(localStorage.getItem('fuse-riders-room-settings-v1')??'{}');return {mode:s.mode,length:s.length,blastWeight:s.weights?.blast};});assert.deepEqual(result.savedPreferences,{mode:'shared',length:2,blastWeight:0});result.settingsRestoredInLobby=true;await host.getByRole('button',{name:'CLOSE',exact:true}).click();
 // Close the guest before adding TV: keep at most two live views on this runner.
 await guestContext.close();const displayContext=await browser.newContext({viewport:{width:1280,height:800}}),display=await displayContext.newPage();observe(display);await display.goto(invite+'&display=1');await display.locator(':is(.online-roster,.room-riders):visible').getByText('Public host',{exact:false}).waitFor();assert.equal(await display.getByRole('button',{name:'ROOM SETTINGS',exact:true}).isVisible(),false);await display.locator('.shared-lobby').waitFor({state:'visible'});await display.waitForFunction(()=>{const image=document.querySelector('.shared-lobby img');return image?.complete&&image.naturalWidth>0;});result.separateTvQrVisible=true;
 await host.getByRole('button',{name:'START RACE',exact:true}).click();await display.locator('.online-notice').filter({hasText:/READY/}).waitFor();await display.locator('.shared-lobby').waitFor({state:'hidden'});await display.waitForFunction(()=>document.querySelector('canvas')?.dataset.renderer==='phaser-webgl');assert.equal(await display.locator('.online-arena').isVisible(),true);result.separateTvVisible=true;await host.getByRole('button',{name:'MAIN MENU',exact:true}).click();await display.getByText('Join your friends, then start the race',{exact:true}).waitFor();await display.locator('.shared-lobby').waitFor({state:'visible'});result.phoneHostControlsTv=true;
 await display.screenshot({path:`${root}/public-shared-tv-${name}.png`});result.pass=true;
 }catch(e){result.pass=false;result.error=String(e);for(const [i,context] of browser.contexts().entries())for(const page of context.pages()){result[`body${i}`]=await page.locator('body').innerText().catch(()=>'<closed>');await page.screenshot({path:`${root}/public-failure-${name}-${i}.png`}).catch(()=>{});}}
 finally{await browser.close();result.errors=errors;result.failedRequests=requests;if(errors.length||requests.length)result.pass=false;results.push(result);await writeFile(`${root}/public-acceptance.json`,JSON.stringify(results,null,2));console.log(JSON.stringify(result));}
}

if(results.some(result=>!result.pass))process.exitCode=1;
