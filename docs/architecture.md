# Fuse Riders architecture

This is the system map for the source tree: what exists on `main` today. Module definitions and tests are authoritative for rules, limits and wire fields; release records describe what was deployed. The [architecture epic #259](https://github.com/andeplane/fuse-riders/issues/259) and the [refactor plan](reviews/refactor-plan-2026-09-17.md) it came from still have open stages — where this map names one, it says so. A stage that is named there and not here has not landed.

## One play path: online rooms

```text
browser replica <------ WebRTC mesh ------> browser replica
         \                                 /
          room service: membership + signalling
               Firestore metadata / Pub/Sub routing
```

Every game is an online room, including solo play (a room with no peers) and a shared screen (a room in shared mode: the TV opens `?room=CODE&display=1` as a display-only member and phones join the same room as controllers). Authority is the shared input log: every device simulates the same deterministic rules locally, and late inputs trigger rollback. The creator supplies initial clock and management authority; the runtime also supports delegated management and peer snapshot recovery. The service never simulates or relays gameplay. `pnpm dev` runs the same room protocol over in-memory metadata (`service/dev.ts`).

The separate LAN server (`src/server/`, a Node process that simulated the game for a TV at `/display` and phones at `/controller`) was removed in [#271](https://github.com/andeplane/fuse-riders/pull/271); those routes no longer exist.

There is one tick driver. `driveGameTick` (`games/fuse-riders/src/engine/tick-driver.ts`) is a tick of a game: `step`, automatic round progression, the room's settings taken over at the round boundary, charges cleared outside play. `applyTick` is a tick of a room: it applies the tick's management entries, folds every rider's log entries and the bots into inputs, and calls the driver. `GameState.settings` is required, so no rule has a fallback that could differ by who built the state, and the checkpoint boundary refuses a state without them ([engine tick driver](design/engine-tick-driver.md)). The second driver this paragraph used to describe, the LAN server's direct calls to `step`, went with `src/server/` in #271.

## Source ownership

| Location                                                                                           | Owns                                                                                                                                                                                                                |
| -------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `games/fuse-riders/src/engine/game.ts`, `state.ts`                                                 | Lifecycle commands and `step` (a loop over `PHASES`); plain game state and its ordered readers                                                                                                                      |
| `games/fuse-riders/src/engine/view.ts`, `view-kit.ts`                                              | `WorldView` and `toView`: the contract rendering sees, rule values included as data; the few kernels it may run                                                                                                     |
| `games/fuse-riders/src/engine/sim/`                                                                | `TickContext`, the ordered `PHASES` list and one file per phase: the tick, and the only place rules run during play                                                                                                 |
| `games/fuse-riders/src/engine/effects.ts`, `pickups.ts`, `weapons.ts`                              | The three registries: one row per timed effect, pickup type and armed weapon, and the readers built from them                                                                                                       |
| `games/fuse-riders/src/engine/tuning.ts`, `geometry.ts`, `rng.ts`                                  | Balance constants and pure functions of them, plane geometry, the seeded random stream                                                                                                                              |
| `games/fuse-riders/src/engine/rider-motion.ts`, geometry and weapon helpers                        | Pure turn-then-move motion, swept contacts, launch and hazard calculations                                                                                                                                          |
| `games/fuse-riders/src/engine/input-log.ts`, `apply-tick.ts`, `tick-driver.ts`                     | Validated log entries, management ordering, held controls, bots; the room's tick and the game's tick it drives                                                                                                      |
| `games/fuse-riders/src/engine/bomb-gesture.ts`, `rider-name.ts`, `avatar-id.ts`                    | The one bomb-input core the log fold uses; the one rider-name guard, seat normaliser and log bound; the avatar ids the log and the checkpoint validate                                                              |
| `games/fuse-riders/src/engine/match-stats.ts`, `shot-log.ts`, `moments.ts`, `leaderboard.ts`       | Match facts, shot outcomes, highlights and session scoring                                                                                                                                                          |
| `packages/fuse-netcode/src/room-runtime.ts`                                                        | Game-agnostic room runtime: the handshake, the tick loop, packets and NACKs, clocks, visibility and frame publication, wiring the collaborators below                                                               |
| `packages/fuse-netcode/src/membership.ts`, `world-sync.ts`, `input-recorder.ts`, `room-manager.ts` | The runtime's collaborators: the member table; the world lifecycle state and snapshot fetch; this device's own entries; seating, presence and succession ([note](design/room-runtime-collaborators.md))             |
| `packages/fuse-netcode/src/stream.ts`, `rollback.ts`                                               | Bounded stream history, completeness, repair and rollback                                                                                                                                                           |
| `packages/fuse-netcode/src/packet.ts`, `snapshot.ts`                                               | Packet decoding and chunked world transfer                                                                                                                                                                          |
| `packages/fuse-netcode/src/game.ts`, `management.ts`                                               | The `RollbackGame` contract a game implements; the shared management entries, succession and permission                                                                                                             |
| `games/fuse-riders/src/online/fuse-game.ts`, `room-runtime.ts`                                     | Fuse Riders behind `RollbackGame`, and its runtime subclass with the rider's controls                                                                                                                               |
| `games/fuse-riders/src/engine/codec/checkpoint.ts`                                                 | Runtime validation of a game state before installation                                                                                                                                                              |
| `games/fuse-riders/src/render/`                                                                    | Phaser scene, presentation time (`time/`), themes and the pure drawing helpers; imports only the engine's view                                                                                                      |
| `games/fuse-riders/src/client/`                                                                    | Audio, avatars, controls, replay, announcer and the stored style choice                                                                                                                                             |
| `games/fuse-riders/src/online/ui.ts` and adjacent UI modules                                       | Online and solo app composition, menus, replay, layouts and diagnostics                                                                                                                                             |
| `packages/fuse-network-fe/`                                                                        | Game-agnostic WebRTC mesh, room API/socket client, link health, ICE recovery and diagnostics                                                                                                                        |
| `packages/fuse-network-be/`                                                                        | Game-agnostic room admission (one game per room), metadata, signalling gateway and backend adapters                                                                                                                 |
| `packages/fuse-network-protocol/`                                                                  | Shared networking wire contract, game ids and authority validation                                                                                                                                                  |
| `packages/fuse-ui/`                                                                                | Shared menus and styles: neon CSS tokens, the element factory, landing, lobby, dialog, controller and account components. Takes sign-in as an interface the game supplies; downloads no SDK and keeps no credential |
| `packages/fuse-platform/`                                                                          | Every game's backend: accounts, identity, match history and settlement, Elo, ratings, leaderboards, storage                                                                                                         |
| `service/`                                                                                         | The one deployed service: composes the room service and `fuse-platform` with every registered game                                                                                                                  |
| `service/history.ts`                                                                               | The served games (`EXTRA_GAME_IDS`) and `Platform`, composed from each game's registration                                                                                                                          |
| `games/fuse-riders/src/platform.ts`                                                                | Fuse Riders' game registration (stats, totals, career, rivals) and the account rules every game shares                                                                                                              |
| `games/dice/`                                                                                      | Pig, the template game: rules (`src/game/`), its DOM page on `fuse-ui` (`src/app/`, served at `/dice/`) and its stats registration (`src/platform.ts`)                                                              |
| `scripts/new-game.ts`                                                                              | Copies `games/dice` to `games/<id>` under a new id, to start a new game                                                                                                                                             |

Packages (`packages/*`) must not import `src/` or `games/`; a game in `games/` may import any package and never another game. The game composes them through `RoomTransport` and related interfaces. Rendering lives in `games/fuse-riders/src/render/` and imports only `games/fuse-riders/src/engine/view.ts` (types) and `games/fuse-riders/src/engine/view-kit.ts`; the rule values it needs travel as data in the `WorldView`, and netcode no longer imports presentation code. See the [render boundary](design/render-boundary.md).

The target is `engine` (pure rules), `net` (simulation coordination), `render` (engine view contract only), and `app` (composition). Renaming folders alone does not establish this boundary. The engine half of that is in place: the simulation lives in `games/fuse-riders/src/engine/`, imports nothing outside itself, and runs each tick as the ordered `PHASES` list ([engine pipeline](design/engine-pipeline.md)). Since #254 the migration allowlist is empty, so the layer-boundary test fails a new cross-layer import rather than pinning it: what the log or the checkpoint guard validates against is the engine's (the rider-name rule in `engine/rider-name.ts`, the avatar ids in `engine/avatar-id.ts`), and `games/fuse-riders/src/shared/` keeps the presentation and the wire vocabulary — avatar labels and atlas cells, the game id, the account and career types, and `protocol.ts`, which re-exports the engine's own primitives rather than owning them. The render half is too ([render boundary](design/render-boundary.md)). The single tick driver ([note](design/engine-tick-driver.md)), the effect, pickup and weapon registries ([note](design/engine-registries.md)) and the app layer with its shared UI (`packages/fuse-ui/`) have all landed. What remains of the layer work is the direction of the imports: app, net and render code deep-imports individual engine modules rather than going through `games/fuse-riders/src/engine/index.ts`.

## Online data flow and recovery

1. The browser creates or joins a short-code room through the room service. A member token identifies its seat; the creator capability authorizes room termination. Public room codes are rendezvous identifiers, not secrets. A member may take a seat or a place in the room's watching list (five each, plus a TV display); a spectator is a folded, listed member with no seat and no inputs ([design note](design/spectators.md)).
2. The service admits members, publishes roster changes and forwards validated SDP/ICE signalling. WebRTC carries reliable control/snapshot messages and unreliable per-tick input packets directly between peers. There is no gameplay relay or TURN fallback.
3. Each member records its own ordered input stream. `applyTick` applies permitted management entries, folds player inputs and bot inputs, then calls `driveGameTick`, which executes the simulation and round progression. Generation and sequence identify reconnects and ordering; a successful send does not prove application by another replica.
4. `StreamLog` tracks retained entries, gaps and completeness. The runtime requests missing entries or rotates retained data. `World` retains rollback state and replays late inputs within its bounded history.
5. A joiner or refreshed device obtains a world snapshot from a peer. Packet, snapshot and checkpoint boundaries validate runtime data; installation rejects invalid state before replacing a healthy world. Live simulation checkpoints are not persisted locally or in Firestore; optional completed-match history is separate from peer world recovery.
6. Presentation consumes snapshots, predicted/interpolated positions and scoped events. It never supplies authoritative collisions, pickups, scores or results.

Two decisions sit on top of that fold rather than beside it, because both had to be deterministic:

- **Who manages the room.** The creator's stream carries the management entries, but `actingCreator` derives the current manager from the folded member list, so management passes to the delegate — the next member in succession order — while the creator's device is disconnected or away, and back the moment it returns. The rule exists once per layer: game-agnostically over `Seat`s in `packages/fuse-netcode/src/management.ts`, and over Fuse Riders' `RoomState` in `games/fuse-riders/src/engine/apply-tick.ts`, which is also where `permitted` decides whether a given stream's entry applies. Every replica computes the same holder from the same log; there is no service-side handover ([ADR 047 §9](adr/047-p2p-input-log-lockstep-rollback.md#9-membership-seats-presence-and-succession)).
- **Starting and rematching.** Readiness is a player log entry, not a host button: every connected human rider votes, at least two riders (AI counted) are required, and the fold checks unanimity after all of a tick's entries, so concurrent votes have one outcome. Votes are cleared on a consumed start or rematch, a return to the lobby, a settings change and a presence transition. Displays, spectators and AI have no vote ([ready check](design/ready-check.md)).

A hidden page steps away by its own entry rather than being judged absent: on `visibilitychange` a seated member logs `STEER 0`/`CANCEL` and then its own `PRESENCE false`, repeated while hidden. Every replica folds that as **away** — the rider keeps its seat across round and match boundaries, is placed in the next round and is rated — while the netcode stops waiting on it, drops it from succession and never asks it for a world. The manager logs its return when it hears the member again ([hidden-tab policy](design/hidden-tab-policy.md), [ADR 047 §12](adr/047-p2p-input-log-lockstep-rollback.md#12-hidden-tabs)).

[ADR 047](adr/047-p2p-input-log-lockstep-rollback.md) records this model in full: packet contents, completeness and the stall rule, rollback bounds, resync triggers, the desync hash, succession, hidden-tab behaviour, the trust model, and a constants table that `tests/p2p-adr-constants.test.ts` checks against the source.

Direct-link failure must surface an explicit retry state. A partial mesh and browser timer throttling remain tracked risks in [#258](https://github.com/andeplane/fuse-riders/issues/258); general mesh tests are not proof of every phone or network condition. Service protocol details and security boundaries are in [PROTOCOL.md](online/PROTOCOL.md).

The service renews room lifetime on any member's admission or valid heartbeat, so a remaining connected rider keeps the room alive after creator departure. A current member's departure starts a 90-second reconnect grace. The creator retains its identity and explicit-end capability; guest renewal does not renew the creator authority grant. Returning devices recover the live world from a peer. Room incarnation and connection fencing reject old callbacks for reused codes or replacement sockets. See the [lifetime design](design/member-kept-room-lifetime.md); partition elections and browser suspension remain separate #258 risks.

## Simulation and time

Simulation state uses ticks; clocks schedule work and rendering samples presentation time. The online clock has one rate, `TICK_MS` per log tick, in every phase. When a round is playing, at least one human rider is seated, none is alive and a bot is, the tick driver runs `BOTS_ONLY_STEPS_PER_TICK` simulation steps per log tick instead of one, so the game runs faster while the clock, the log and the network cadence do not. `stepsPerTick` decides the count as a pure function of folded state, so every replica takes the same one and a rollback that changes it replays the ticks after it ([ADR 047 §11](adr/047-p2p-input-log-lockstep-rollback.md#11-game-speed-when-only-ai-survive), [design note](design/fixed-clock-game-speed.md)). `RoomState.tick` counts log ticks; `GameState.tick` counts simulation steps.

`games/fuse-riders/src/engine/rider-motion.ts` applies steering before movement at a fixed simulation step. Bots emit ordinary inputs through `BotController`; they do not receive special collision or movement rules. The seeded RNG is `engine/rng.ts` and the pinned trigonometry `engine/deterministic-math.ts` (on `@stdlib` kernels, so the result does not depend on the browser's `Math`); both are in the engine, not in `shared/`. Room settings, pickup definitions and game constants are the sources for balance; this guide intentionally does not duplicate numeric balance tables.

## The tick contract: `PHASES`

`step` in [game.ts](../games/fuse-riders/src/engine/game.ts) holds no rules. It builds one `TickContext` and walks `PHASES` in [pipeline.ts](../games/fuse-riders/src/engine/sim/pipeline.ts). **That list is the tick contract**: it is the only statement anywhere of what happens in which order, and reading it is how you find out. Nothing here restates its entries; the [engine pipeline note](design/engine-pipeline.md) has the phase-by-phase table.

A phase is `{ name, when, run(ctx) }`. It may read and write the game state and this tick's shared working set through its `TickContext` — which lives for exactly one tick and is never stored, so anything a later tick or another replica needs has to be in `GameState` — push events, and state facts (a `DeathFact`, a gun hit, an observation) for a later phase to act on. It may not read a wall clock, schedule work, touch the input log, the network, storage or presentation, or use unseeded randomness: `state.tick` is the only time and the seeded stream in `rng.ts` the only randomness, and neither `Math.random` nor `Date.now` appears anywhere under `engine/`. `when: "always"` phases run whatever the game phase; with no round in play the tick ends at the first `when: "playing"` phase, after the clock, the expiries and the countdown.

The order is part of the rules, not an implementation detail: phases hand each other half-finished state (the sweep detects against uncommitted positions, `commitDeaths` is the one place a rider dies, and no phase before `recordFacts` reads statistics), so moving one changes outcomes. Changing the order is therefore a rules change — bump `RULES` and refresh the golden in the same commit, as [below](#rules-and-the-golden). `games/fuse-riders/tests/golden-hash.test.ts` is what makes that enforceable rather than advisory: it replays a recording that exercises every mechanic and compares the hash on every tick, so a reordering that was meant to be a pure refactor fails there.

In broad strokes the ordered list runs:

1. Advance the clock, expire transient fields and blasts, start play when the countdown ends.
2. Age and clip trails, fit the field to the closing walls, move the scenery that moves, attempt a scheduled pickup spawn.
3. Compute every rider's step, collect pickups, bounce immune riders, fly shells, explode due fuses and their chains.
4. Sweep the steps against projectiles, blasts, walls, scenery, trails and each other; resolve shields and portal transits. Nothing has been committed yet.
5. Commit positions and trails, then the sweep's deaths.
6. Apply weapon inputs against the committed board; resolve guns in the same tick; commit those deaths; observe dodges.
7. `recordFacts` writes match statistics, the shot log and highlight moments from the facts the phases stated; `resolveRound` decides the round.

Every death goes through one `commitDeaths` over `DeathFact`s, and no phase before `recordFacts` reads statistics, so they cannot steer an outcome. `step` returns events only; callers that need the public snapshot call `toView`. A phase that throws surfaces as a `TickFault` naming the tick and the phase; the state is still left part-way through the tick, as it always was, and recovering from that is tracked separately (#253 C8).

What a phase means is a registry row, not a branch inside it. `EFFECTS`, `PICKUPS` and `WEAPONS` hold one row per timed effect, pickup type and armed weapon; a rider carries `effects: ActiveEffect[]` and `armed: WeaponKind[]`, a gun bullet resolves on the tick it is fired and leaves a `TracerState` in `GameState.tracers` rather than a zero-range bomb, and the effect readers are generated from the table so none of them names a kind. Adding one is a row plus, for a weapon, its launch hook ([engine registries](design/engine-registries.md)).

### `RULES` and the golden

`RULES` in [apply-tick.ts](../games/fuse-riders/src/engine/apply-tick.ts) identifies compatible simulation rules. A rules change requires a version change; a source refactor must preserve behavior, and `games/fuse-riders/tests/golden-hash.test.ts` holds it to that on every tick of a recording that exercises every mechanic ([engine safety net](design/engine-safety-net.md)).

## Rendering and controls

Phaser is externally stepped by the presentation owner. It renders supplied state and cosmetic fractional time; it must not own authoritative physics, timers or a second render loop. The online runtime supplies interpolated and predicted state. Presentation must freeze or reset cosmetic history across appropriate scope, phase and discontinuity boundaries. See [PHASER.md](PHASER.md) for lifecycle, timing and recovery contracts.

Input state, pointer ownership and transport delivery are separate. Press, release and cancel edges must survive buffering and ordering; blur, hidden-page transitions, disconnect and teardown must neutralize controls without accidentally firing. Host capabilities are authenticated by the room service; snapshots and public invites must not expose them. Input streams use room, member generation, tick and sequence ordering; snapshots and emitted events also carry match/round context. Changes must preserve lifecycle boundaries so stale inputs or outcomes cannot affect a new game.

Themes and UI geometry are cosmetic. Keep the shared-screen display and phone controller modes, the neon/pixel aesthetic and simulation identity intact while sharing app components. Presentation exceptions must not silently become parser failures or corrupt the simulation loop; the existing callback seams are tracked in #255/#258.

## Service and deployment boundaries

Production runs the room service on Cloud Run with Firestore room metadata and Pub/Sub cross-instance signalling. `service/dev.ts` uses in-memory adapters locally and in CI. See the [backend package](../packages/fuse-network-be/README.md), [deployment guide](online/GCP-DEPLOY.md), and [release inventory](online/DEPLOYMENT.md).

One service and one Firestore database serve every game, keyed by `gameId`. A room serves one game; match records, ratings, leaderboards and the history routes (`/api/games/:gameId/…`) carry the game, and an unknown game is refused. The account is shared across games. Fuse Riders is the legacy game: an absent `gameId` means it, its routes keep their unprefixed URLs, and its rating and totals stay on the user document; other games keep theirs in `${prefix}-ratings`. See [`fuse-platform`](../packages/fuse-platform/README.md) and the [multi-game design](design/multi-game.md).

Validate admission, signalling and stored records at runtime; bound queues, dedupe maps and recovery. Preserve exact-origin checks, capability authentication, stateless gateways and incarnation fencing. Origin checks are not authentication. The abuse and hosting-cost changes in [#256](https://github.com/andeplane/fuse-riders/issues/256) are pending work, not guarantees made by this map.

## Verification boundaries

[Verification guidance](verification.md) maps unit, browser and release checks to their scope. Tests use typed injected clocks, schedulers, transports and randomness; browser checks exercise the actual presentation and transport seams. Coverage applies only to `.c8rc.json`'s included modules. Neither a passing PR verify job nor desktop browser emulation proves physical-phone or WAN acceptance. Claims about releases require the exact source revision and live external evidence.
