# Coverage exclusions

The unit gate includes `service/**`, `packages/*/src/**` and `games/*/src/**` by default. New source files automatically join the gate. `.c8rc.json` lists exact-file exemptions so an untested module cannot silently exempt its whole directory. Declaration files (`**/*.d.ts`) contain no executable behavior.

This makes coverage scope explicit; it does not claim these excluded modules are safe or tested. Browser smokes and production-adapter checks are separate evidence. Existing line/statements/functions and branch thresholds are unchanged. Six already-tested helpers previously omitted from the gate are now included: arena announcements, blast animation, trail debris, status copy, pickup rarity and room-code validation.

| Excluded file                                           | Why / removal criterion                                                                                                                                                                                                                                                          |
| ------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `games/dice/src/app/main.ts`                            | The dice page's DOM glue: it puts the tested presenter, session and report modules on the page. Covered by `scripts/dice-smoke.ts`; needs a DOM fixture before removal.                                                                                                          |
| `packages/fuse-network-be/src/gcp/firestore-store.ts`   | Production persistence or messaging adapter; needs injected Firestore/PubSub tests before removal.                                                                                                                                                                               |
| `packages/fuse-network-be/src/gcp/index.ts`             | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |
| `packages/fuse-network-be/src/gcp/pubsub-bus.ts`        | Production persistence or messaging adapter; needs injected Firestore/PubSub tests before removal.                                                                                                                                                                               |
| `packages/fuse-network-be/src/index.ts`                 | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |
| `packages/fuse-network-fe/src/index.ts`                 | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |
| `packages/fuse-network-fe/src/peer-transport.ts`        | Browser WebRTC integration; needs injected RTC and timer coverage (#258). Existing policy tests do not cover this module.                                                                                                                                                        |
| `packages/fuse-network-fe/src/transport.ts`             | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |
| `packages/fuse-network-protocol/src/index.ts`           | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |
| `packages/fuse-network-protocol/src/wire.ts`            | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |
| `packages/fuse-platform/src/firestore.ts`               | Production persistence or messaging adapter; needs injected Firestore/PubSub tests before removal.                                                                                                                                                                               |
| `packages/fuse-platform/src/index.ts`                   | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |
| `games/fuse-riders/src/client/avatar-heads.ts`          | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/client/rider-colors.ts`          | The avatar picker's twin, and additionally unimportable under `node --test`: `import "./rider-colors.css"` has no loader outside Vite. Lift that out and it becomes testable; covered today through the runtime and the colour dialog tests.                                     |
| `games/fuse-riders/src/client/game-audio.ts`            | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/client/main.ts`                  | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/client/powerup-guide-view.ts`    | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/client/replay-overlay.ts`        | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/client/viewport-lock.ts`         | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/account-panel.ts`         | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/friends-panel.ts`         | The friends dialog's DOM glue: it puts the tested client (`friends-client.ts`), notifier (`friends-notify.ts`) and fuse-ui dialog (`packages/fuse-ui/src/friends.ts`) on the page with the game's endpoints and sign-in. Needs a DOM fixture in the Node harness before removal. |
| `games/fuse-riders/src/online/account.ts`               | Firebase browser authentication integration; needs typed SDK/browser test seams.                                                                                                                                                                                                 |
| `games/fuse-riders/src/online/analytics-setting.ts`     | DOM for the SETTINGS privacy row; its wording and states are covered in `analytics-consent.ts`. Needs a DOM fixture in the Node harness before removal.                                                                                                                          |
| `games/fuse-riders/src/online/attract.ts`               | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/connect-hint.ts`          | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/endpoints.ts`             | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/join-form.ts`             | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/dialogs/room-settings.ts` | The ROOM SETTINGS dialog around `showRoomSettings` (itself excluded); the registry it opens through is covered.                                                                                                                                                                  |
| `games/fuse-riders/src/online/keyboard-shortcuts.ts`    | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/landing.ts`               | The landing page's DOM (moved out of `ui.ts`). Its routing decisions are `landing-route.ts`, which is covered; needs a DOM fixture for the page before removal.                                                                                                                  |
| `games/fuse-riders/src/online/mobile-play-layout.ts`    | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/room-settings-menu.ts`    | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/telemetry.ts`             | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/online/ui.ts`                    | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/render/ink-renderer.ts`          | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/render/phaser/arena.ts`          | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/render/phaser/presentation.ts`   | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/render/portal-palettes.ts`       | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/render/power-indicator.ts`       | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/render/reload-ring.ts`           | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `games/fuse-riders/src/render/themes.ts`                | Browser presentation or app composition currently outside the unit gate; add focused behavior tests and remove the exemption during #254/#255.                                                                                                                                   |
| `service/index.ts`                                      | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |
| `service/listen-free.ts`                                | TCP listener startup integration; needs explicit binding/retry tests.                                                                                                                                                                                                            |
| `service/room-limits.ts`                                | Entry point, re-export or contract module; cover executable initialization/lifecycle behavior before removing this exemption. Pure types need no behavioral tests.                                                                                                               |

## Player stats rendering

`games/fuse-riders/src/online/player-stats.ts` creates DOM/SVG only and is exercised by `scripts/player-stats-smoke.ts` in Chromium and WebKit at desktop and phone widths. Its aggregation, Elo and parsing logic lives in covered shared/service modules. Remove this exact-file exemption when the Node coverage harness supports the browser DOM/SVG fixture; browser coverage is not implied by the unit percentage.
