# Online completion audit — 2026-09-14

**The online game is deployed and its core room/play flows work. The broader request for demonstrated smooth, resilient online play is not fully verified yet.** Calling the release a beta communicates limits; it does not complete missing requirements or pass acceptance gates.

Scope: the user's four online-room requests, subsequent benchmark/local-response request, GCP/Pages and direct-only decisions, developer documentation/reviews/CI requirements, and current AGENTS/ADRs/ROADMAP. This audit reads existing evidence and source only; it did not start browsers, rebuild assets or interrupt the running soak. The root's active-goal state is not exposed by this subagent's goal tool, so no goal-completion status was changed.

## Requirement-by-requirement status

| Requirement | Implemented and verified | Still needs proof or resolution |
| --- | --- | --- |
| Choose shared TV versus full game on each device | Creation/settings UI selects `shared`/`devices`; joined shared controllers hide their arena while separate display renders it. Local browser room/AI flows verified on desktop and phone-sized layouts. | Final deployed shared-display plus phone-host flow; physical phones are unverified. |
| Host creates a room, joins on a phone and starts/reset games | Host capability, invitation/QR, separate display identity, start/reset/settings and add/remove AI implemented. Initial public Chromium created a room, added AI, joined a guest and reached scoring over RTC without page errors. | Repeat complete product acceptance on the final published bundle, especially actual phone-host touch/background behavior. Initial public WebKit reached scoring but logged an RTC-send error. |
| Host preferences: match length, powerup toggles/weights, localStorage | Validated settings, first-to-wins/fixed rounds, zero-weight disable, normalized probabilities and host localStorage are implemented and unit/browser checked. Format versus next-round application is documented. | Final public save/reload/recover check must distinguish saved defaults from an already recovered match. No evidence of physical-browser storage/lifecycle testing. |
| Minimal backend, rooms online | Pages frontend and Cloud Run backend both published at source `66f67bd`; backend revision `00003-qf7`; complete public provider smoke passed. Firestore owns room/membership/lease metadata; Pub/Sub handles signalling only. Real two-local-process provider tests exercised cross-process signalling. | Two actual Cloud Run instances/revisions plus killed-gateway/cold-reconnect evidence is not established by the public smoke. Read-only routing/IAM inspection is not that test. |
| WebRTC gameplay; failed connection is explicit | No gameplay route through WSS/Pub/Sub; RTC probes, bounded retries, stale scopes and shot-failure notice implemented. Unit and application-boundary outage checks verify safety. | Direct RTC cannot connect on every NAT/network; no TURN configured. This is the user's accepted failure/retry scope, not a guarantee that arbitrary network problems disappear. Final failure/retry UI and resume need deployed browser evidence. |
| Local movement should respond immediately and feel smooth | Shared fixed-tick kernel, intended/applied ledger, held replay, conservative clock, bounded prediction and coherent remote rendering are implemented with deterministic tests. | Actual input-to-first-changed-pose p95 and phone-to-TV p95 are not established. Existing next-frame hook alone is insufficient. Local presentation is predicted; host collisions/deaths/scores remain authoritative, so “locally true” cannot mean irreversible client-owned positions. |
| Benchmark poor networks, bandwidth and performance | Reproducible seeded application-send impairment harness; preserved failed traces; reviewed liveness/keyframe fixes; one exclusive 30-second poor-profile run passed unchanged two-second recovery/freshness checks. | Current regional/soak results, correction/traffic distributions and actual network/phone evidence are still separate work. The single visible desktop renderer does not certify five physical phones/TV. |
| Good README, AGENTS, architecture, ADRs and reviews | Prominent Play link, setup/architecture, provider names/accounts/limits, source/image evidence, rollback instructions, ADRs and independent reviews exist. | Historical ADR031 still describes the superseded Cloudflare/relay provider; ADR032 still contains superseded fallback wording. Reconcile status/current-reading guidance without deleting history. Some dated documents call older evidence “current”; use the latest inventory update. |
| Public correctly named repository, atomic commits and CI | Read-only GitHub query confirms public `andeplane/fuse-riders`. Source `66f67bd` CI `34792860482` completed successfully; 302 local tests and enforced coverage passed. Scoped commit history records independent features/fixes. | Documentation-only CI for the later checkpoint was running at audit handoff. Final deployed artifact identity must match whichever revision is claimed tested; later source existence is not publication evidence. |

## Running or queued work is not passed work

The root reports an isolated 30-minute soak against unchanged `66f67bd` assets is running. Regional and final public-browser acceptance are queued, not running, in the root's work plan; no completed final report was available to this audit. A phone-sized browser benchmark configuration is being added for use after the soak; that work is not yet a benchmark result. Do not mark those checks passed until outputs, exact artifact identities and assertions are inspected. The latest backend public smoke is already a completed pass, separately recorded in [GCP inventory](../online/GCP-INVENTORY.md#matching-source-update-0038-utc).

Even a successful existing soak has a narrower scope than the full proposed ADR032 soak: six connected contexts with one visible renderer, eliminated riders waiting for subsequent rounds, repeated ordinary steering/fire workload and bounded diagnostic queues. The harness does not measure CPU/memory growth or assert that target aiming, gun impacts, persistent shells and a shrinking arena were all exercised. It cannot alone certify those conditions, uninterrupted five-alive play, physical-device FPS or real wire loss.

## Acceptance gaps that must remain explicit

1. **Final artifact browser quality:** rerun public Chromium and WebKit with the actual published release manifest; require clean page-error results. Test shared TV, device mode, phone-sized host controls, settings persistence and reconnect/reset. The initial WebKit `pass: true` describes completed flow assertions despite a recorded error, not clean acceptance.
2. **Response and rendering:** measure actual changed local pose (candidate p95 ≤33 ms), phone gesture to changed TV pose (candidate p95 ≤100 ms), and declared physical iPhone/Android frame distributions. Single-render desktop p95 around 17–18 ms is useful evidence but not a substitute.
3. **Poor-network/traffic evidence:** finish and preserve current representative profiles; report alive-player correction p95/p99/max/frequency at the intended RTT, per-view average/p95 one-second payload traffic and host total. Account for wire overhead separately. Existing injection budgets or latest-value metrics do not prove the proposed ≤0.5 Mbps average/1 Mbps p95 limits. Real packet loss/congestion needs a verified network impairment layer; pre-SCTP message dropping is a different test.
4. **Lifecycle and consistency:** physical background/lock/unlock, Wi-Fi/mobile transition and bfcache; deployed duplicate host/revision coexistence and gateway termination; impaired application-level agreement on confirmed outcomes. Typed stale-scope, duplicate-shot, checkpoint and cross-room regressions are already positive evidence, not a substitute for all these boundaries.
5. **Operational limitations:** runtime subscription create/get/consume/delete permissions remain project-wide, with topic attachment/publish scoped to the game's topic. The documented residual permission risk is not a fully prefix-scoped IAM result. Rollback commands exist but an executed compatible rollback rehearsal is not recorded. Neither issue is cured by a beta label.

ADR032 still labels its numeric gates proposed. This audit does not silently promote all proposals to new user requirements, waive them, or mark them passed. Before declaring full completion, record which measurable contract is accepted and supply its evidence; unverified physical/hardware gates must remain unverified if access is unavailable. The explicit direct-only decision removes WSS fallback/relay throughput and cross-person host migration from the required solution. It does **not** remove the user's poor-network, responsiveness or benchmark requests.

## Achievable software checks versus external evidence

The protected soak, queued regional/public-browser runs, phone-sized viewport benchmark, valid changed-pose/traffic summaries and documentation reconciliation can be completed in this environment. Physical iOS/Android touch-to-photon, hardware frame performance, lock/network transitions and verified OS-level packet impairment require device/network access not established here. Missing access leaves those claims unverified; it does not itself demonstrate a product defect. Phone-sized WebKit/Chromium is still worthwhile software evidence but cannot replace physical device measurements.

## Immediate completion sequence

Finish the protected soak, inspect assertions and publish its bounded conclusions. Complete the pending regional/final public-browser runs without weakening thresholds. Extract response/correction/traffic distributions from valid instrumentation or add the missing measurement after the soak. Reconcile historical ADR status and publish a final exact-revision evidence summary. Obtain physical-device/real-network evidence where possible; otherwise report the specific unverified requirements and leave full qualification incomplete rather than redefine success around beta availability.
