# Independent implementation review: direct-action core

Date: 2026-09-14. Scope: `src/shared/direct-input.ts`, `src/online/direct-stream.ts`, and `src/online/rollback-world.ts` against amended ADR041. Tests were still being written at initial inspection. Runtime, clock/coordinator integration and online release are outside this review's completion scope.

## Initial findings

1. **Earlier watermark constraints are discarded.** `DirectStream` retains only its newest watermark. Accepting `(5,1)`, then `(10,2)`, then actions `(sequence=1,tick=3)` and `(sequence=2,tick=4)` currently returns accepted throughout. The second action violates the earlier promise that no sequence after 1 can occur through tick 5. This was reproduced with actual exported module calls. Retain bounded unfinalized cuts or equivalent interval constraints; validate existing records and future arrivals against them, including when older cuts arrive late. Trim constraints only when their meaning is preserved by the finalized base or a truly stronger equivalent constraint.

2. **A newer incomplete cut erases earlier completeness.** With action 1 at tick 3 and cut `(5,1)`, `completeThrough(5)` is true. Receiving cut `(10,2)` before action 2 makes it false, even though completeness through tick 5 remains proven. This was reproduced through exported module calls. Select a retained certified cut that proves the requested tick, rather than requiring possession through the newest unverified watermark. Otherwise future packet loss unnecessarily delays finality and can force an avoidable pause.

3. **Bootstrap permits an impossible active gesture.** A valid game plus held bomb flag and gesture `{active:1, latest:2}` is admitted by `RollbackWorld.open(packBootstrap(...))`. The reducer can only produce `active=0` or `active=latest`: a newer press replaces the prior active gesture. Validate this invariant and retain rejection atomicity. The executable reproduction used an ordinary game with default room settings and a roster entry; no private state mutation was used.

## Positive observations and remaining gates

Per-tick folding preserves player iteration order, command-local aim, replacement cancellation, and matching release semantics. Stream admission builds a candidate before installing it, validates identities and tick order, and only exposes contiguous records. Rollback restores strictly before newly admitted actions; event storage is rebuilt for replayed ticks and only emitted on verified finality. Exact finalized prefixes are distinct from newer completeness cuts, and same-finality duplicates are checked without re-emitting events.

The initial findings require fixes and regressions before approving this core. Completed real-MessagePack tests must cover delivery permutations, last-release and receipt loss, same-tick gesture/aim order, watermark history/finality, bounds, corrupt bootstrap and six-replica convergence. These tests do not establish real WebRTC latency, full runtime replacement, mobile performance or release acceptance.

## Fix verification and disposition

Approve the inspected core as the foundation for integration. All three initial findings are resolved. `DirectStream` now retains at most 64 unfinalized completeness cuts, validates new and retained actions against every cut, rejects incompatible older cuts, and keeps earlier certified progress usable when a newer cut has missing records. Finality trimming preserves a baseline sequence/tick fence and later cuts; encoded cut storage is included in the world budget. Bootstrap admission now requires an active gesture to equal the latest gesture, or to be zero.

Independently ran `node --import tsx --test tests/direct-actions.test.ts` against the inspected working-tree sources: 19 tests passed, zero failed, zero skipped. Regressions reproduce both older-promise arrival orders, preservation of certified progress despite a newer gap, cut overflow/trimming, and rejection of a rehashed impossible gesture state. Additional tests cover full absolute ticks, bounded real MessagePack decoding, sequence gaps/conflicts, late whole-world rollback, all six same-tick packet permutations, replacement gestures, idle final-release repair, lost receipts, pending finality, exact checkpoint prefixes, speculative pause/resume, malformed boundaries, atomic encoded-history overflow, and six replicas across three seeds.

This approval is deliberately a core implementation disposition. The six-replica test uses in-process deterministic delivery and a short fixed workload; it does not establish sustained active gameplay, cross-browser behavior, actual WebRTC delivery, clock quality, TV/controller runtime roles, or measured latency/heap/wire costs. Complete repository checks, actual integration, sustained impairment evidence and independent runtime review remain required for the requested online replacement. No public release is approved.

## Same-room mesh signalling amendment

Approve the inspected changes to `src/service/gateway.ts` and `worker/index.ts`. Removing the host-only pair restriction at GCP ingress and bus delivery, and at Worker routing, permits current same-room guest pairs to negotiate direct links. Existing room/incarnation, current source and target connection, target membership, signal schema, expiry, size/rate and bus deduplication fences remain. Gameplay payloads still cannot use the signalling route; this does not add a gameplay relay or change lease ownership.

Independently ran `node --import tsx --test tests/service-room.test.ts tests/worker-authority.test.ts`: 34 passed, zero failed or skipped. Modified regressions prove guest-to-guest delivery with authenticated source connection, rejected stale targets, delayed old-source signals after replacement, foreign-room targets and gameplay-shaped signalling payloads. Existing authority, metadata/bus fencing, expiry and relay rejection tests also passed. No correctness blocker found in this bounded routing amendment. Real SDP/ICE mesh establishment and dual-channel browser behavior remain separate integration gates; no production deployment was inspected or approved.

## Initial browser transport inspection

Inspected `PeerTransport` mesh/fast-channel changes and `scripts/direct-mesh-browser.ts`; the parent was running the browser harness, so this review did not start another copy. The lexicographically smaller peer initiates offers/restarts. Compact binding records include the authority epoch/incarnation and both current connection identities; current-link callback checks reject callbacks from replaced RTC objects. Fast ingress checks the 512-byte packet boundary, exact action/receipt shape and alias before dispatch, with bounded receive tokens. Runtime bootstrap/ownership validation remains responsible for authorizing calls to `bindFast`.

Two corrections are required before transport approval. The reliable `game` channel currently checks its label but not its ordered/reliable delivery settings; admission must verify ordered delivery and absent retransmission/lifetime limits. Also its closing/close/error handlers drain only the reliable gate, while `sendFast` checks only the fast gate. That leaves a fast-send window after an early reliable closing signal during the known WebKit association shutdown ordering. Drain both required-channel gates on either terminal channel signal, or otherwise apply the reliable drain fence to fast sends. Preserve current-link checks so stale channel callbacks cannot drain a replacement.

The harness appropriately describes coordinator *application callback* blocking while lease/liveness traffic continues. Its all-direction sends can establish actual fast-channel delivery when completed, but its stale-alias check currently exercises sender rejection rather than forged stale ingress. It does not yet establish channel-close/restart recovery, connection/epoch replacement, sustained loss/rate behavior or integrated gameplay. Those remain distinct evidence gates. In particular, bot/aim workloads must be checked against the 100-message/second per-link limit; several streams owned by one coordinator share that limit.

The ordered/reliable channel validation and shared drain corrections were subsequently inspected and are resolved. Both required channels now drain both gates on terminal signals with current-link checks; `fastReady` and `sendFast` share the same readiness predicate.

Further membership/async inspection found a replacement race requiring correction. The welcome handler waits for ICE configuration, then emits peer-online callbacks and offers from its original roster without rechecking the current socket/admission and current peer connection identities. An offline or replacement message during that wait can therefore be undone by a late online callback. Likewise, `link(id)` checks the socket across its ICE await but not the remote connection identity. An old-source signal waiting for ICE can resume after replacement and construct a new RTC object using stale SDP, then route its response to the current replacement connection. Capture and validate current local/remote connection identities across async ICE/offer/description boundaries, and filter resumed welcome work against current membership. Object-identity callback checks alone cannot fence an obsolete operation that creates a new object after replacement. Add delayed-ICE/membership regressions before transport approval.

The subsequent identity-fence changes resolve this inspected race: resumed welcome processing checks the current socket/local admission and each original peer's current connection ID; link construction captures/rechecks local and remote connection IDs across ICE loading; signal processing checks the returned RTC object's current identity. A `linkReset` callback also permits fresh alias handshakes after RTC recreation. The expanded browser harness now includes stale-alias ingress, action-channel closure/recreation and a held ICE request during another peer's replacement. A removal-without-replacement test should additionally assert no obsolete online callback/offer after ICE release; merely observing eventual replacement convergence is weaker than asserting stale work was suppressed.

## Compact liveness probe design amendment

Approve replacing steady-state reliable-envelope probes with bounded fast tuples `[1,segment,8,probeId]` and `[1,segment,9,probeId]` after alias binding, provided the following rules are implemented. Third values 8/9 are reserved outside receipt slots 0–4 and use exact four-element validation, uint32 IDs, the same scope/512-byte/rate checks, and separate transport dispatch rather than application action delivery. Keep the existing 200 ms probe cadence, outstanding-ID bounds and 600 ms acknowledgement validity initially; measure their loss sensitivity now that delivery is unreliable. Count encoded bytes in both probe directions.

Probe and pong sends must bypass the current-health predicate used for ordinary gameplay sends, while retaining bound identity, open-channel, drain and small probe-buffer checks. Otherwise a short loss burst marks the link unhealthy and prevents the probes required to restore health. Deferred pongs capture and recheck both their RTC object and received segment after the macrotask, so a new binding cannot relabel an old request. Continue answering reliable bootstrap probes on their reliable lane even after local fast binding; asymmetric handshake progress must not deadlock. Reliable bootstrap probes remain available before binding. These probes establish link liveness only; they do not certify held controls, action possession or simulation completeness. Re-run blackhole/recovery, asymmetric binding, stale/deferred pong and channel-close evidence for this semantic change before transport approval.

An asymmetric-binding browser failure subsequently demonstrated that reliable replies alone are insufficient: a peer may bind locally before its own health allows the reliable bound acknowledgement, while the other peer still drops unbound fast probes. Approve the proposed correction to send both compact and reliable bootstrap probes until a validated incoming compact probe or pong proves the remote knows the exact current alias. Track this as remote alias readiness separately from health; only a valid pong for an outstanding issued probe establishes acknowledged delivery. Reset readiness for each new alias, authority scope or RTC association. Continue reliable-lane replies throughout. Stale aliases cannot confirm readiness. The parent retained the failing browser evidence as `artifacts/direct-mesh-browser-asymmetric-binding-failure.json`; successful rerun and code inspection remain required.

The implemented `remoteConfirmed` correction was inspected and matches this amendment. Exact-scope compact liveness marks remote alias readiness; only `LinkHealth.acknowledge` can turn a received issued-pong ID into delivery evidence. Fast probe sends bypass gameplay health gating while preserving scope, drain and probe-buffer checks. Reliable replies remain reliable, and deferred fast pongs retain their received segment. No additional protocol blocker was found in this inspection.

Transport acceptance remains pending: the inspected latest `artifacts/direct-mesh-browser.json` records `Error sending binary data through RTCDataChannel.` in its page-error list. The parent reports the expanded connectivity/blackhole/closure/membership assertions completed before that final gate failed and is collecting phase/stack diagnostics. Passing those assertions does not turn this run into a clean pass. Resolve or make an explicit independently reviewed scope decision about the browser error, preserve the failing evidence, and rerun the relevant acceptance gate before approving the transport implementation.

## Input-origin and clock amendment review

Reviewed the proposed input-origin/clock integration amendment before implementation. The origin direction is appropriate: separate UI revisions from wire sequences, omit unchanged resends, stamp absolute next-tick actions independently, preserve explicit gesture edges, and prepare an entire local update before publishing it. The clock remains separate from rendering and simulation mutation, with bounded outstanding probes/samples, monotonic progression and explicit stale/fault states.

One clock rule requires correction before implementation approval. The proposal accepts RTTs up to 500 ms but latches a recovery fault whenever a newest midpoint estimate differs by more than four ticks, before selecting the lowest-RTT sample. A valid 480 ms response whose delay is almost entirely on one leg can have 4.8 ticks of midpoint error, falsely faulting an otherwise accurately running client. Validate discrepancies using the sample's uncertainty interval rather than treating its midpoint as truth, and account for uncertainty in the current alignment; alternatively specify another bounded rule that proves accepted network jitter cannot cause this false fault. A noisy newest sample must not defeat the stated lowest-RTT estimator. Regression evidence must include this concrete asymmetric-delay case alongside a real clock discontinuity.

The amendment also needs these implementation clarifications:

- Sample a fractional coordinator tick immediately before sending the scoped reply. Define pre-start/readiness behavior explicitly: a future segment start cannot yield playable ticks before the bootstrap base or admit controls before activation.
- A stale/faulted clock must block both new controls and completeness advancement. A release that could not be timestamped must lead to visible cancellation/recovery and cannot be queued to fire when freshness returns. Define how local input state is neutralized and a fresh physical gesture becomes eligible after recovery.
- Reject or explicitly define contradictory local fields such as a press edge with `bomb:false`; otherwise separate origin adapters could interpret the same update differently.
- Prepare the origin, local stream and every required subscriber queue before publishing any group bytes. A later transport enqueue failure retains the admitted records for paced repair; it cannot undo actions already published to another peer. Existing `DirectDelivery.enqueue` publishes immediately, so runtime integration needs an explicit prepare/commit boundary or equivalent proven group preflight.

Clock/timestamp implementation approval is pending these amendments. They preserve the direct-action target and do not introduce coordinator admission for normal inputs.

The revised input-origin/clock contract was subsequently inspected and is approved for implementation. Discrepancy validation now compares the newest and previous selected sample uncertainty intervals at one observation time; a high-RTT midpoint cannot alone override jitter selection or trigger the four-tick fault. Pre-start followers require a reply sampled after the coordinator reaches the bootstrap tick. Input/completeness blocking, failed-release cancellation/recovery, contradictory-frame rejection and preparing every group candidate before publication are explicit. The stale-baseline greater-than-four-tick rebootstrap is a conservative recovery policy after missing clock history, not proof of a physical clock jump. Implementation tests must also show that later fresh samples cannot revive a failed release before its required lifecycle/control recovery completes. Runtime activation and implementation evidence remain pending.

## Input-origin and clock implementation disposition

Approve the inspected `src/online/direct-origin.ts` and `src/online/direct-clock.ts` modules for integration. Origin updates are nonmutating candidates with separate UI/wire sequences, normalized aim, strict contradictory-frame rejection, explicit gesture edges and exact-cut checks. A suspended origin cannot issue actions or watermarks even after its clock recovers. The clock implements scoped outstanding probes, interval-based discrepancy checks, lowest-RTT filtering, bounded monotonic slew, pre-start sender confirmation and latched faults without stepping the world.

Independently ran `node --import tsx --test tests/direct-origin-clock.test.ts`: 11 passed, zero failed or skipped. Tests exercise real MessagePack groups, edge filtering, replacement/missing gestures, watermarks, invalid frames, future starts, the 480 ms asymmetric-RTT regression, slew in both directions, stale/expired/reordered replies, scope fencing, clock discontinuities, exhaustion and a suspended release that remains ineligible after fresh samples. Runtime integration must still enforce clock `canOriginate` before both input and watermark operations, suspend when an untimestampable release occurs, prepare every required delivery/replica candidate before publishing bytes, and enact the lifecycle recovery barrier. The modules' isolated tests do not prove those runtime responsibilities.

The separately inspected transport startup adjustments publish initial membership synchronously before ICE loading and reuse an already negotiating or candidate-only RTC object for repeated initial offers. These changes are coherent with the earlier identity fences. Their role in any prior untraced browser failure is not established; clean repeated browser evidence and the native-send investigation remain separate gates.

The subsequent `DirectDelivery.prepare`/`publish` helper is approved for integration. Preparation copies the subscriber queue and checks the complete group without changing the original or sending bytes. Publication selects the retained admitted records, includes bounded older repair records, and leaves the group pending when transport enqueue fails. Callers must install all subscriber candidates in one synchronous transaction before calling publication; the helper does not create cross-runtime atomicity by itself.

Independently ran `node --import tsx --test tests/direct-actions.test.ts tests/direct-origin-clock.test.ts` after this addition: 31 passed, zero failed or skipped. The added regression verifies original-queue isolation, complete encoded group contents, failed-send retention, repair at 50 ms rather than 49 ms, and rejection of a sequence gap without changing retained state. No new blocker found in the helper.

## Runtime wiring amendment review

Reviewed the proposed runtime wiring amendment against the goal of replacing the actual online path. `DirectSegment` composition, a compact bound reliable lane, creator-owned management with delegated full-view simulation, and geometry-free controller status are the right direction. The compact reliable lane is acceptable with a closed discriminator set, exact per-message validation, the 2 KB bound, current authority/connection/alias fences and existing send gates. This does not authorize activation before the lifecycle clarifications below or qualify the outstanding native-send behavior.

One newly identified interaction requires amending the earlier origin cut rule. With continuous aim/steering changes, the latest issued action is usually stamped one tick ahead. An independently scheduled 100 ms progress pump using the current clock tick can repeatedly fail `tick >= lastActionTick`, causing a healthy active origin to hit the 500 ms silence barrier. Timer or render-event phase alignment cannot be a correctness prerequisite. Publish the exact cut `(currentTick, lastSequenceAtOrBeforeCurrentTick)` while preserving already-issued future records. The receiver's exact-cut rules already allow sequences after that cut at later ticks. Add a bounded per-tick origin frontier and a delivery API that accepts this exact cut, with an adverse-phase continuous-aim regression. Publishing cuts into the future instead would add avoidable input delay and is not the recommended correction.

Before lifecycle/runtime activation, make the following contracts explicit:

- Clock, finality and controller status come only from the exact delegated coordinator connection/generation; action slots are authenticated against immutable origin owners; receipts come only from subscribed simulators for their stream. Admission to the room alone does not confer these roles.
- Barrier prepare/ready/activate/applied messages carry explicit generation/transfer IDs with duplicate/stale handling, bounded retry/retention and timeout. A scheduled start that is no longer safely usable after all required readiness receipts must be rescheduled or aborted, never activated inconsistently.
- Same-match coordinator handoff cannot install a base older than any receiving simulator's finalized prefix/history. Otherwise an apparently valid older checkpoint can undo irreversible scores/events. Include all already finalized history or use the explicit fresh-lobby recovery path when it is unavailable.
- Neutralization clears simulation charge/target state as well as held flags and active gestures. Keep management intentions bounded and scoped; the existing one-recent-command/five-second policy is a suitable baseline rather than indefinite queuing while a display is absent.
- Controller status carries segment/tick/revision fences and distinguishes provisional availability/geometry from finalized outcome fields. Define change filtering and a rate bound so status does not recreate routine bulk metadata traffic. Stale status cannot disable controls for a newer scope or promote a speculative death to a finalized result.

These are integration contract requirements, not a request to retain a host relay or another permanent opt-in runtime. Approval of the new lifecycle orchestration remains pending their incorporation and evidence.

The revised runtime wiring contract was subsequently inspected and is approved for implementation: it now states delegated coordinator/slot/subscriber authorization, bounded management intentions, explicit barrier identity/deadline behavior, comprehensive neutralization, monotonic finalized-history handoff and scoped/change-filtered controller status. The origin's bounded current-tick frontier and delivery exact-cut API also match the progress-starvation correction. The new orchestration regression deliberately processes continuous input before every watermark pump. Integrated implementation approval remains pending its completed tests and the finality refinement below.

## Pending-finality starvation refinement

The parent found a second composition-level liveness defect: replacing the only pending certificate with each newer coordinator proposal can prevent a lagging replica from finalizing an already reconstructable earlier tick while later inputs remain incomplete. Approve replacing that single pending slot with at most 40 per-tick certificates, strictly after current finality and within its 40-tick horizon. Apply the highest currently complete/reconstructable certificate, retain newer ones, prune certificates at or below newly committed finality, and include encoded certificate storage in the byte budget. The ADR's earlier single-pending statement must be amended accordingly.

Retained same-tick certificates must be immutable; normalize prefix pair order for semantic duplicate comparison. A now-checkable invalid prefix/hash must not be silently bypassed because a newer certificate happens to be valid. Keep rejection/overflow atomic and emit each newly finalized event once. Required regressions cover a continuing stream of newer incomplete certificates while earlier finality advances, same-tick conflicts and reordered duplicates, retained-certificate bounds/budget behavior, and event deduplication across multi-tick finality advancement. This is a reviewed change to preserve useful progress under bounded loss, not a relaxation of finality correctness.

## Repair scheduling and publication refinement

Approve bounded rotating repair selection to avoid repeatedly spending every spare slot on the same early records while receipts lag. A fresh edge group remains first and immediate; include the earliest unacknowledged record unless already present, then rotate remaining repair slots by stable sequence IDs. ACK removal and candidate copying must not reset rotation to the same tail. Keep the four-record/512-byte packet cap, 50 ms repair pacing and 256-record retention bound. Add lost-receipt regressions proving both earliest-gap coverage and tail progress, not only eventual success after action loss. All records remain retained until receipt or explicit recovery; rotation does not weaken delivery semantics.

Approve finality publication as soon as a newly complete tick becomes eligible, at no more than 10 Hz. An unsuccessful attempt to propose finality must not consume the 100 ms publication opportunity. If a new eligible certificate arrives during the rate window, retain it and flush when due even if no further event arrives. A failed transport enqueue retains a certificate for bounded retry and cannot undo already committed local finality. These changes remove avoidable scheduling delay without publishing physics state every tick.

The existing combined 100-message/second fast bucket is insufficient for legitimate input, receipts, cuts and liveness. A replacement must be derived from explicit supported per-stream production bounds and the five authenticated slot ownership/subscription relationships on each link. For illustration, a declared 60 Hz new-input bound plus 20 Hz repair and 10 Hz cuts gives at most 450 messages/second across five action/receipt stream roles, plus 10 probe/pongs; a 500-message/second combined budget could cover that declared envelope with a finite burst. This is not approval to silently restrict the currently tested 100 Hz aim workload to 60 Hz. Specify its actual bound or reviewed UI coalescing policy first. Separate a small liveness allowance from action/receipt traffic, preserve an overall byte/message ceiling, validate stream ownership before using slot allowances, and measure bot/continuous-aim workloads before freezing the final limits.

## Direct segment implementation review

Inspected the new `DirectSegment` orchestration and revised origin, delivery and rollback modules. The bounded current-tick frontier, stable rotating repair cursor, immutable normalized per-tick finality certificates and eligible-publication pacing implement the reviewed refinements. Segment configuration copies/freezes the roster and excludes the bootstrap bytes from retained configuration. Slot owners gate action ingress, subscribed views gate possession receipts, and the selected coordinator gates clock replies/finality. Locally originated groups prepare every subscriber candidate before world admission and publication. These are coherent integration foundations; connection/delegation authorization still belongs to the pending browser adapter.

Independently ran `node --import tsx --test tests/direct-actions.test.ts tests/direct-origin-clock.test.ts tests/direct-segment.test.ts` against the inspected working-tree sources: 40 passed, zero failed or skipped. The added tests cover finality progress beneath newer incomplete certificates, normalized duplicates/conflicts, invalid earlier hashes, retained certificate count/encoded-byte limits, event deduplication, rotating repairs under lost receipts, exact cuts excluding future groups, a six-segment 100 Hz workload with deterministic fast loss/reordering, five controller origins with one TV simulator, and immediate direct peer actions while coordinator control traffic is blocked. This remains short in-process integration evidence, not browser/runtime acceptance or a measured human bandwidth result.

Two corrections are required before segment implementation approval:

1. `snapshot()` copies finalized phase/winners/scores but leaves `players[].alive` and `roundPlacements` from speculative state. This exposes tentative deaths/placements to outcome UI and the planned lightweight controller status, contrary to ADR041. Copy the finalized outcome fields or expose a distinct finalized status source that consumers must use. Keep current geometry provisional. Add a regression with a provisional elimination that becomes public only after verified finality.
2. Playable clock uncertainty and the four-tick future-record fence are inconsistent. Independently reproduced this with two live segments, two owned slots, a neutral tick-65 checkpoint, 10 ms scheduler observations, a 480 ms delay only for follower-to-coordinator clock requests, and immediate fast actions/replies. The follower accepts a roughly 490 ms RTT sample with roughly 4.9 ticks of uncertainty. Its ordinary steering input at 600 ms returns success, but the coordinator immediately faults `World invalid — synchronizing`: the correctly generated next-tick action is five ticks beyond the coordinator's local target. No loss, forged data or clock discontinuity is involved. The existing isolated asymmetric-RTT clock test therefore does not prove the composed admission contract. Define and test a bounded relationship between playable alignment uncertainty, next-tick stamping and permitted future ingress, including two followers with opposite estimation errors. Either constrain readiness before issuing actions/cuts or review a finite larger admission envelope; removing the future fence is not acceptable.

The parent acknowledged the outcome-field correction and is implementing its regression. Clock/admission alignment remains pending a reviewed correction. No browser test was started by this reviewer. Rate policy, actual RoomRuntime wiring/lifecycle regressions, complete repository checks, native-send investigation and independent runtime/release review remain separate gates.

### Clock uncertainty and future staging amendment

Approve a fixed global uncertainty contract as the simplest bounded correction, subject to implementation/regression inspection. A playable clock may have at most five ticks of uncertainty, measured as the selected sample's radius plus the distance from its projected midpoint to the current clock projection. A larger value yields an explicit `uncertain` state: no new actions, completeness advancement or world advancement until qualified again, with existing bounded progress/recovery behavior. This retains the stated 500 ms sample envelope without imposing a new 150 ms RTT readiness cutoff. Do not classify incoming action timestamps as corrupt using an unqualified stale/uncertain target; leave those packets for existing bounded repair. Latched clock faults still require visible recovery.

Derive the remote future-record staging bound from both participants' maximum uncertainty plus permitted origin lead. With five ticks for each clock and the existing at-most-four-tick origin lead, that is **14 ticks** beyond the local target. The current segment's strict next-tick path would need only 11, but adopting 11 without tightening the broader origin contract would leave an undocumented exception. No per-packet clock metadata is necessary when all admitted participants share this fixed bound. Preserve the origin's own four-tick limit, four-record/512-byte packets, 256-record queues, byte budgets and 40-tick execution/finality horizon. Future staging does not execute actions early or extend rollback history.

Required regressions compose actual origin/clock/stream delivery: the reproduced asymmetric coordinator/follower case, two followers with opposite accepted estimation errors, exact permitted/rejected future boundaries, and an uncertain sample that cannot issue controls/cuts or advance the world. A clock-quality recovery must not revive a failed release or an old segment. This is an explicit reviewed change to the earlier four-tick *remote admission* rule; it does not authorize silently weakening its old assertion without replacing that assertion with the new derived boundary evidence.

Approve a related initial-progress grace of 1,500 ms from the coordinator's segment start until each origin's first strictly newer validated cut (`throughTick > baseTick`). Post-start sender-time confirmation can consume a 500 ms clock RTT plus probe phase, followed by first-cut scheduling/transit; the normal 500 ms silence deadline cannot be assumed to start before that exchange finishes. After the first advancing cut, retain the existing 500 ms deadline since strictly advancing progress. Base/duplicate cuts and action packets cannot reset the initial deadline or count as initialization. Keep the independent 40-tick speculative ceiling. Regression evidence must cover successful delayed startup, failure when an origin never advances within the initial grace, and the shorter silence deadline after its first advancing cut.

The subsequent implementation inspection confirms the finalized death/placement fields, shared 5/4/14 uncertainty/lead/staging constants, qualified fast-ingress target, per-origin startup grace, controller finality conflict fence and stopped-state check after event callbacks. Independently reran the three direct-action/origin-clock/segment test files: 47 passed, zero failed or skipped. New evidence includes opposite follower estimation errors exceeding nine ticks, staging without early execution, the exact 14/15-tick admission boundary, unchanged 40-tick execution limit, provisional elimination visibility and startup/steady silence handling.

One residual correction is required: uncertainty currently becomes zero when every clock sample expires. Using the added test's accepted samples, `read()` reports uncertainty 14.5 and `canAdvance:false` at 970 ms; it remains uncertain at 1,500 ms, but at 1,980 ms returns stale, uncertainty zero and `canAdvance:true` without receiving another sample. Preserve uncertainty disqualification across sample expiry until a new bounded observation qualifies it. Previously qualified idle clocks can retain their separately allowed bounded stale advancement. A sample-expiry regression is needed before this correction is approved.

## Authenticated fast-flow budget amendment

Approve the declared design envelope of 100 new input groups, 20 repair sends and ten completeness-cut sends per second per owned stream. Bind each RTC association to explicit authenticated incoming action-slot and receipt-slot sets from the immutable roster; reject overlap and unknown slots, with at most five combined slot flows. Each authorized flow receives 140 messages/second with burst 280. Liveness receives a separate 20/second with burst 40. The overall fast allowance is 750/second with burst 1,500, retaining the 512-byte packet maximum. All five legitimate flow rates/bursts plus liveness fit beneath the overall limit. These are admission ceilings, not expected gameplay bandwidth.

Repeated same-alias binding must not refill budgets; retain the overall association bucket across alias changes. Over-budget traffic is dropped for bounded repair/recovery and reported separately from malformed gameplay. Actual UI/bot production must honor the declared 100-group/second envelope. Implementation evidence must cover slot/direction authorization, overlap rejection, token rate/burst/refill boundaries, liveness under action load and five simultaneous 100 Hz flows. Browser/runtime activation remains pending these checks; this amendment does not approve the old combined 100/second bucket or qualify transport behavior by itself.

### Ingress implementation disposition

Approve the inspected `DirectIngress` implementation and its `PeerTransport.bindFast`/fast-ingress integration. Permissions are copied into disjoint slot/direction flows, same-alias rebinding is idempotent, and the overall/probe buckets survive alias changes. The association budget and packet size are checked before parsing; current alias/association and exact tuple checks precede authorized flow admission. Unauthorized flows are ignored. Exceeding the overall or flow rate drains/closes the required channels with an explicit retry status rather than reporting a world corruption. Reserved liveness capacity covers legitimate authorized flow load; it does not promise continued service from a peer flooding malformed/unauthorized packets into the pre-parser association limit. Runtime must still derive bind permissions from its authenticated roster.

Independently ran `node --import tsx --test tests/direct-actions.test.ts tests/direct-origin-clock.test.ts tests/direct-segment.test.ts tests/direct-ingress.test.ts tests/direct-control.test.ts`: 54 passed, zero failed or skipped. Ingress regressions cover slot/direction/overlap rejection, permission copying, exact burst/refill, five 100 Hz flows plus repair/cuts over ten simulated seconds, reserved probe capacity, packet limits and association-budget preservation across aliases. Compact reliable control schema tests also passed. No browser test was run by this reviewer; real mixed-engine transport and actual online activation remain separate gates.

Persisting the last measured clock uncertainty fixes the reported expiry case, and its 970/1,500/1,980 ms regression passed. One caller-order robustness improvement remains recommended: update persisted uncertainty on clock/sample observations, including acceptance, rather than only in `read()`. Otherwise an accepted uncertain sample can expire through other clock calls before the next read, leaving the previous qualified measurement cached. Add a variant that omits the read immediately after uncertain acceptance so this invariant does not depend on an undocumented caller schedule.

The caller-order improvement was subsequently inspected and is resolved. `rememberUncertainty` now runs after projection/sample updates during observation and after accepted samples, retaining disqualification when no samples remain. Independently ran `node --import tsx --test tests/direct-origin-clock.test.ts`: all 15 passed, including expiry through `request()` calls without an intervening read. This closes the remaining clock review note. Approve the inspected segment/core corrections and authenticated ingress modules for continued runtime integration; there are no outstanding blockers in this bounded review scope. Actual browser adapter lifecycle/delegation wiring, complete acceptance evidence and release review remain required for the requested online replacement.

## Concrete runtime startup/handoff composition

The proposed connection-scoped creator delegation, current-coordinator checkpoint handoff, lightweight controller catalog/status and future-start applied-acknowledgement barrier fit the approved architecture. Requiring every admitted member ready/bound is stricter than requiring only simulators, but remains coherent with a bounded visible timeout. An ephemeral lobby validator on a controller creator must not become a retained or ticking gameplay world. This review does not approve the implementation before it is inspected.

Preserve three timing/state details during wiring. Check missing applied acknowledgements and their deadline before every coordinator clock reply/start observation, not only a scheduled abort timer; after a delayed wakeup, an RTC callback must not publish a started tick before noticing an expired barrier. Freeze the old coordinator before selecting/copying its finalized handoff base, preventing a later certificate from overtaking the checkpoint. Validate the actual pre-neutralization base/hash against each existing view's finalized fence, then derive the neutral replacement; an unattached claimed old hash next to mutated state is insufficient. If the current coordinator/base is unavailable, retain the explicit paused/fresh-lobby recovery path rather than guessing a higher fence.

## Initial actual-runtime cutover review

Inspected the new default `RoomRuntime`, UI routing and direct room-state helpers. The online path now constructs direct segments, sends independent inputs over authorized mesh flows and keeps full checkpoints away from controller-only segment instances. The lifecycle payload carries the real old checkpoint followed by validated management operations; views validate their prior finalized fence before deriving neutral replacement state. The coordinator checks the applied-acknowledgement deadline from clock-receive, input and tick paths, rather than relying only on a timer. These are implementation improvements, but runtime approval is pending the following concrete corrections and regressions.

- **Untimestampable release bypass:** the runtime's early `canOriginate` return originally bypassed `DirectSegment.input`, whose failure path suspends the origin. Showing a failed-shot notice alone leaves the previous gesture eligible when uncertainty clears. Freeze/recover or delegate to the segment failure path, and test release during uncertainty followed by a fresh sample at the actual runtime adapter boundary.
- **One-shot recovery liveness:** `recover()` originally sent one request, set `recovering`, and then prevented retries or timeout. Failed enqueue or an unavailable coordinator could leave the participant frozen indefinitely. Retain a single scoped request with at most two retries/second and a bounded visible timeout; clear it on replacement/authority changes. Test failed-first-enqueue recovery and coordinator absence.
- **Source without verified base:** `prepare()` falls back to a locally constructed catalog world whenever its source has no simulator state. A refreshed delegated TV starts with a new empty catalog but can be selected again by peer ID. Its unrelated match ID bypasses the same-match fence and can silently reset an active game. Delegation must distinguish an existing-base handoff from explicitly authorized fresh-lobby initialization. A replacement source without the required base must pause for explicit recovery. Test delegated-TV refresh during play and same-peer/new-connection/no-base handling.
- **Header/candidate consistency:** full-view preparation originally checked checkpoint hash/tick/match/owners but not derived round or the complete thin status. Activation uses header phase to select `running`, so a valid playing checkpoint with a structurally valid lobby header can stop simulation despite passing the hash check. Verify the derived round/status against the header, or derive those activation fields exclusively from the validated candidate. Add phase/round/status mismatch rejection tests.

The parent is implementing release/recovery corrections and running the actual browser smoke; this reviewer did not start a concurrent browser run. Authority reconnect, setup sequencing and large-checkpoint/backpressure cases remain runtime evidence requirements, not inferred from the helper tests.

### Exact lobby catalog reconstruction

Approve retaining the lobby tick and ordered historical leaderboard alongside identities, seed and settings for exact reconstruction of a canonical lobby. Preserve raw Map insertion order, including departed historical players; a sorted presentation leaderboard cannot reproduce the canonical checkpoint hash. The simulator supplies this exact lobby catalog to controller creators. This is a lobby-only reconstruction mechanism and must not become a fallback for an in-progress game.

Independently ran `node --import tsx --test tests/direct-room-state.test.ts`: six passed, zero failed or skipped. The added played-to-lobby round-trip reproduces the checkpoint hash at nonzero tick with historical entries whose score order differs from insertion order. Existing tests cover neutral transition derivation, actual pre-mutation finalized fences, strict snapshot boundaries, controller geometry exclusion and role/ownership validation. These helper tests do not cover the runtime failure paths above.

### Bounded runtime-fix verification

Inspected the four corrections and approve them in the tested scope. An unavailable-clock release now freezes the segment and requests recovery. Recovery retains one scoped request with 500 ms retry spacing and a five-second visible unavailable state. Creator-authenticated plans distinguish initialization from existing-base preparation; a replacement delegated TV without its required world reports base unavailability instead of constructing a new game. `prepareWorld` checks the complete derived thin status, match/hash, owners and optional exact lobby catalog before installation. Role discovery now waits for all current service peers, and management processing waits for outgoing application acknowledgements.

The new typed `RuntimeEnvironment` and `RuntimeTransport` permit tests of the actual public runtime with injected scheduling, clock, storage and serialized delivery, without private-field mutation or global patches. Independently ran `node --import tsx --test tests/direct-runtime.test.ts tests/direct-room-state.test.ts`: 12 passed, zero failed or skipped. These cover direct guest input, failed-first-enqueue recovery, unavailable-coordinator timeout, an untimestampable release across recovery, delegated-TV replacement during an active countdown, exact lobby reconstruction and header/checkpoint inconsistency rejection.

This closes the four reported corrections in that bounded scope. Overall runtime acceptance remains pending the creator-refresh race under investigation, authority-scope transitions, recovery-attempt classification and chunk/backpressure evidence. In particular, ordinary link/visibility/membership recovery must not consume ADR041's corrupt/hash-failure budget unless an explicit policy amendment is reviewed. No concurrent browser test was run by this reviewer.

## Recovery-policy separation amendment

Approve separating the rolling three-corrupt-recovery-attempts/30-second budget from ordinary membership/link synchronization. Only explicit checkpoint/hash-conflict categories consume that budget, and one failed candidate/attempt cannot be charged repeatedly by timer retries. Preserve the rolling corruption history across ordinary successful synchronization; success may reset a recovery episode, not erase recent corruption attempts.

Add a 15-second unsuccessful recovery episode deadline spanning replacement prepare attempts, plus a five-second queue/response deadline for coordinator-to-creator plan requests. Retries/new attempts cannot restart the existing episode clock. Timeout produces a visible creator MAIN MENU/fresh-lobby path rather than a healthy-connection notice. This supplements the existing per-attempt barrier deadlines and does not relax the corruption threshold.

For a running segment, declare recovery successful only after activation/application completion, a qualified clock, and verified finality advancing beyond the new base; controllers may use their authenticated coordinator finality. Clock qualification alone is insufficient: repeated starts can qualify clocks while never obtaining a complete origin cut, continually resetting the episode before another two-second speculative pause. A stationary lobby can use application completion and clock qualification because its simulation does not advance. Regression evidence must cover four healthy link rebuilds within 30 seconds, absent creator/start responses, and continuously clock-ready replacements that never finalize. Implementation review remains pending.

### Recovery implementation inspection

The inspected implementation retains a 15-second episode across attempts, adds the five-second plan-request deadline, requires post-base finality before resetting running recovery, and preserves corruption history across routine successful resynchronization. Connection replacement clears that peer's management deduplication state. Prepared phase/status publication and cache writes now wait for activation.

Independently ran `node --import tsx --test tests/direct-runtime.test.ts tests/direct-room-state.test.ts`: 17 passed, zero failed or skipped. The added cases cover four successful rebuilds within 30 seconds, a continuously clock-ready/no-action-cut episode reaching its bound, a missing creator response ending retries, creator refresh in a settled lobby, and guest refresh with a new connection/command sequence. Browser lifecycle/startup failures remain separate unresolved evidence.

Two boundary corrections remain before recovery-policy implementation approval. Corruption charging currently recognizes the generic `World invalid` string, which also represents invalid/future/reused-gesture action ingress; use an explicit operation/cause category so unrelated input faults cannot consume the checkpoint/hash budget. Also `requireLobby` leaves current-alias activation callable: a delayed `directActivate` can install a segment and set the recurring status to Connected after timeout has latched `recoveryRequired`. Fence current-alias activation/status/normal control once that unavailable state is latched; leaving it requires an explicitly accepted replacement/fresh-lobby decision. Add non-corruption input-fault accounting and delayed-activation-after-timeout regressions, including persistence of the visible timeout state after the notice hold expires.

The subsequent explicit `WorldResult.corrupt` signal correctly distinguishes verified finality/prefix/hash disagreement from invalid action admission and propagates through `DirectSegment` to runtime recovery. The latched timeout now fences current-alias object messages, compact control/fast delivery and local input, while a new authenticated creator plan remains eligible. The ownership-header correction also correctly compares slot-to-owner mappings independently of sorted presentation order while preserving bootstrap player iteration order.

Independently ran `node --import tsx --test tests/direct-actions.test.ts tests/direct-segment.test.ts tests/direct-runtime.test.ts tests/direct-room-state.test.ts`: 55 passed, zero failed or skipped. New regressions cover corruption-versus-future-input classification, four invalid-input recoveries without exhausting the corruption budget, ignored late activation after timeout, and departure/seat-removal/rejoin with changed player insertion order.

One narrow classification omission remains: a conflicting already-installed preparation header sets the runtime fault but does not mark it corrupt. This is a checkpoint metadata conflict and should explicitly charge the same per-attempt checkpoint/hash budget. Mark that branch and cover it without reintroducing generic text-based action-fault classification. Browser/startup and full acceptance evidence remain separate from this bounded correction review.

Approve the proposed direct-runtime lobby-boundary settings correction: when validated management ends in lobby and the resulting game's settings differ from the session's selected settings, append an explicit settings operation before constructing the transition checkpoint. The active round retains its existing settings until that boundary, while the new lobby and its catalog agree exactly. Keep this in the direct transition journal without changing HostSession or LAN semantics. Verify the exact nonzero-tick state/hash through settings-change-during-play, return to lobby and creator epoch refresh; matching UI labels alone is insufficient.

The final metadata-conflict classification and lobby-settings corrections were inspected and are approved. A conflicting installed preparation now explicitly marks corruption; stale old-alias duplicates cannot add another charge. The direct management journal applies the selected settings once its result is lobby, preserving active-round behavior and leaving HostSession/LAN unchanged.

Independently reran the direct actions, segment, runtime and room-state test files: 56 passed, zero failed or skipped. The new metadata conflict regression checks one charged attempt followed by an ignored stale duplicate. The creator-refresh regression now changes settings during active play, returns to a nonzero-tick lobby, changes authority epoch and verifies every full view retains the exact same finalized hash. These close the outstanding source corrections in this bounded runtime review. Chunk/backpressure qualification, strengthened actual Chromium/WebKit lifecycle checks and the complete acceptance suite remain pending; no release approval is implied by these unit/in-process results or the parent's first successful Chromium smoke.

## Checkpoint backpressure inspection

Approve the inspected idle-send gate and checkpoint routing. `permitsIdle` requires an open action channel, an active monotonic send gate and zero queued fast bytes. `sendCheckpoint` additionally uses the existing reliable send's authority, membership, reliable-channel, health, visibility and buffer checks. It can operate before alias binding while still yielding to queued action traffic. The runtime routes checkpoint chunks through that method, preserves the current offset after refusal, and retains its two-at-most-12-KB-chunks per 50 ms pacing and per-transfer size/time limits.

Independently ran `node --import tsx --test tests/link-send-gate.test.ts tests/direct-runtime.test.ts`: 19 passed, zero failed or skipped. The new tests cover missing/non-open/buffered/drained action channels, blocked transfer without premature phase publication, and resumed transfer under the same alias. Native RTC boundary behavior remains with the parent's browser harness; no concurrent browser was started here.

The persistent-backpressure test's creator-at-22-seconds/guest-at-27-seconds result exposes an episode-accounting gap despite eventual bounded failure in that fixture. A creator can issue each replacement plan just before the guest's own five-second preparation timeout. The guest then never calls local `recover()` and never starts its 15-second unsuccessful episode. Remotely superseded incomplete attempts still belong to the same unsuccessful availability episode; merely extending the guest assertion does not establish the reviewed cross-alias invariant.

Start/retain a participant's episode when a newer plan supersedes its previous preparation/activation before the existing applied/qualified-clock/post-base-finality success criterion is met. Replacement aliases must not postpone that participant's deadline. The initial five-second attempt followed by the 15-second episode remains an acceptable bound, but add a focused regression with replacements just before each local attempt timeout and assert the guest's own episode limit rather than relying on the creator eventually stopping. This accounting correction remains required before closing persistent-backpressure acceptance.

The proposed settled-scope marker is an appropriate correction: retain success independently of `freeze()` so an ordinary management change after healthy play is not misclassified as failure. Scope that marker to incarnation/epoch/revision, or clear it on authority changes. A pending attempt begins when a coordinator-bearing plan is adopted, including the wait for its first preparation header. Requiring `incoming` to exist misses a connected source that never sends a header: the current receiver has no preparation timeout in that state. Track adoption time, enforce its five-second wait, and treat a superseded never-settled plan as incomplete even before a header arrives. Coordinator-null waiting-for-TV plans remain intentionally idle. Required regressions additionally cover a missing header and repeated replacement plans without headers. Source correction and final disposition are still pending.

### Backpressure and episode correction disposition

Approve the inspected correction and close this bounded review. Each coordinator-bearing plan now starts its preparation deadline on adoption, including the header wait. A settled marker uses the exact adopted plan object's identity and is written only after the existing applied/clock/finality success criterion; reused numeric aliases cannot inherit another plan's success. Superseding any never-settled coordinator plan starts/retains the episode, while a frozen previously healthy plan retains its completed status. The persistent-backpressure regression now requires both creator and guest to reach their own bounded unavailable state without adding a trailing five-second grace to the guest assertion.

Independently ran `node --import tsx --test tests/link-send-gate.test.ts tests/direct-runtime.test.ts`: 22 passed, zero failed or skipped. Added regressions cover repeated replacements just before local timeout, absent preparation headers and repeated plans without headers. The parent retained the pre-fix failure in `artifacts/checkpoint-episode-before.log` and reports successful native mixed-browser idle-gate checks. The inspected latest mesh artifact records a passed six-context transport run with no errors, while remaining explicitly separate from integrated gameplay and physical-network qualification. No browser was run by this reviewer. There are no remaining findings in this checkpoint backpressure/episode scope; full byte/performance measurement and complete runtime acceptance remain separate work, with no release approval implied.

## Acknowledged plan-delivery optimization

Approve replacing the steady four-Hz hello/full-plan exchange with bounded acknowledged plan delivery. Guests retry hello only until a validated current plan confirms their exact connection and display role. The creator retains one acknowledgement set and per-member retry timestamp for its current plan, retrying only unacknowledged current members at most every 250 ms. Successful enqueue does not acknowledge delivery. New plan/authority scope replaces this bounded delivery state; self-adoption can satisfy the creator's own acknowledgement without a wire exchange.

Guests acknowledge semantically identical validated current-plan duplicates so lost acknowledgements recover without reinstalling state. Conflicting same-revision content and stale plans cannot be acknowledged as accepted. Accept a plan acknowledgement only for the exact current revision/member through existing authority/current-connection fences. It proves plan delivery, not checkpoint readiness, activation/application or clock permission. Duplicate hellos must not reset retry pacing or restore unconditional replies to already acknowledged peers. Existing preparation/activation/recovery deadlines still govern unavailable participants.

Implementation regressions must cover failed initial enqueue, lost acknowledgement, duplicate acknowledgement without state reinstallation, stale/wrong-connection/revision acknowledgement, replacement membership, and absence of steady full-plan/hello traffic after acknowledgement. Preserve the parent's pre-optimization full-game measurement as baseline and remeasure all payload classes; the reported savings are not accepted evidence until the new implementation and measurement complete.

### Plan-delivery implementation inspection

The inspected current-plan delivery map, current-member/revision/connection acknowledgement fences, canonical duplicate acknowledgement and validated-role hello suppression satisfy the intended contract. Failed enqueues remain pending, duplicate hellos preserve pacing, and plan/authority replacement discards old acknowledgement state. Delivery acknowledgement remains separate from preparation and activation.

Independently ran `node --import tsx --test tests/direct-runtime.test.ts`: 23 passed, zero failed or skipped. The new regressions cover steady metadata silence, a failed first plan enqueue and lost acknowledgement, stale/foreign acknowledgements with duplicate hello bursts, and conflicting/stale versus identical current-plan duplicates. Existing tests retain creator/guest replacement and bounded recovery coverage. No browser or build was run by this reviewer.

One narrow pacing correction remains: the duplicate `directPlanRequest` branch still directly sends the full plan. A delegated coordinator repeats that request every 200 ms while awaiting activation, bypassing the reviewed 250 ms unacknowledged-only delivery policy and resending metadata after delivery was acknowledged. Route that duplicate branch through the same `sendPlans()` policy and cover repeated delegated-coordinator requests before activation. This is a setup metadata/pacing finding; it does not invalidate the observed settled-game hello suppression. Integrated traffic measurements and overall release acceptance remain separate.

The duplicate request branch now uses `sendPlans()`, closing that finding. Independently reran `node --import tsx --test tests/direct-runtime.test.ts`: 24 passed, zero failed or skipped. The new shared-TV regression withholds a controller's readiness, observes repeated delegated coordinator requests while the barrier remains pending, and verifies the acknowledged TV receives only one full plan. Approve the acknowledged plan-delivery implementation within this bounded scope; no remaining findings. The parent's post-change gameplay measurements and all-view outcome/source-identity checks remain separate acceptance evidence and were not run by this reviewer.

## Intentional idle-lobby recovery cancellation

Approve the proposed cancellation of an inherited simulation recovery episode when an authenticated, validated coordinator-null plan is adopted. This is the explicit shared-lobby boundary where no simulation is expected and participants may wait indefinitely for a display. It cancels an obsolete attempt rather than declaring successful simulation recovery. Preserve rolling corruption history and the existing deadlines across all coordinator-bearing replacement plans. Clear any obsolete delegated `pendingPlan` request at this idle boundary too: normal activation clears that request, but this branch has no activation to do so.

The parent reproduced the inherited-deadline defect with a failing injected-runtime regression and retained `artifacts/shared-idle-before.log`. Require the regression to remain idle for 20 seconds without a recovery latch and then admit a TV successfully, alongside the unchanged repeated-plan deadline tests. Implementation verification is pending. The separate WebKit creator-refresh/shared-TV failure must be rerun before claiming this finding explains its complete failure.

Inspected and approve the resulting narrow correction: only the validated coordinator-null adoption branch clears `recoveryEpisodeAt` and `pendingPlan`; corruption history is untouched. Independently ran `node --import tsx --test tests/direct-runtime.test.ts`: 25 passed, zero failed or skipped. The new regression interrupts a prior simulation attempt, waits in the intended shared lobby for 20 seconds without a simulation or recovery latch, then successfully admits a TV. Existing repeated coordinator-plan, missing-header and corruption-budget tests also pass unchanged. No remaining findings in this idle-boundary scope. No browser or build was run by this reviewer; the parent’s WebKit rerun remains separate evidence.

## Preliminary one-second combined-pulse design review

The proposed quiet-traffic reduction is feasible, but slowing existing independent timers is insufficient. The current one-second clock freshness, 600 ms path health and 500 ms origin progress deadlines conflict with a one-second exchange. Treat the following as concrete design recommendations pending the complete ADR042 contract and implementation review, not acceptance of changed source.

Elect exactly one initiator per current RTC pair: a follower initiates its coordinator pair, otherwise use stable peer-ID order. Use an alias/association-scoped monotonic request ID and an immediate reply. Include acknowledgement of the responder's prior reply in the next request, including retry requests. Receiving a fresh request alone proves inbound traffic, not successful outbound delivery; both endpoints need newly validated acknowledgement of their own issued pulse before refreshing bidirectional health. Duplicate accepted acknowledgements cannot refresh health indefinitely. Retain authenticated bootstrap probes until both endpoints explicitly confirm pulse mode, and reset that mode on scope/association replacement. Bounded pulse/reply sends must retain the health-independent probe send path so expired application health cannot prevent its own recovery.

A practical initial budget is a quiet request deadline of at most one second, immediate reply, and missing-response retries every 250 ms with at most four attempts per second and eight outstanding IDs. Give each retry a fresh attempt ID to avoid retransmission ambiguity in RTT samples. Separate the approximately 2.5-second health evidence window from the clock's existing maximum 500 ms RTT admission: the responder normally receives acknowledgement of its reply in the next periodic request. Retain immediate native channel, authority and visibility fences and the separate sustained association restart policy. Aggregated pulse parsing still needs a closed shape, authorized owned-cut/received-prefix slots, one total 512-byte cap and a bounded control-class ingress allowance; aggregation must not bypass flow ownership checks.

Extending clock sample freshness to 2.5 seconds also requires explicit uncertainty growth under a stated supported relative drift bound. For example, a deliberately chosen 500 ppm assumption adds 0.01 tick of radius per second; it is an operating assumption to validate, not a browser guarantee. Age the interval used for sample selection and discrepancy checks, retain uncertainty across sample expiry, and preserve the five-tick playable cap. A sample with exactly 500 ms RTT has no drift margin below that cap. Startup keeps rapid clock acquisition and emits its first cut immediately on qualification instead of waiting another second.

Publish current exact cuts on real action edges, pulse requests/replies, first clock qualification and bounded progress demand. Send receipts when a contiguous prefix advances or a duplicate repair needs re-acknowledgement; no independent quiet receipt timer is needed. Do not wait for a second heartbeat phase to publish finality: emit an eligible certificate immediately after at least 20 ticks of finalized progress, or after any positive progress while an outcome/headroom request is pending. Piggyback when a packet is already due; otherwise use a compact event-driven certificate. Tentative deaths/outcomes should demand confirmation immediately so presentation does not acquire an avoidable one-second result delay.

A headroom demand around 24 provisional ticks, coalesced by alias/target/frontier and retried no faster than every 250 ms, is a reasonable low-latency starting profile. It is not a proof of uninterrupted execution across every admitted 500 ms RTT/asymmetric-clock case. Keep the 40-tick execution cap and allow conservative earlier demand/adaptive cadence under adverse delay, or explicit bounded pause when the cap is reached. One second of cut phase plus one second of certificate phase already exhausts that window before network transit or clock uncertainty. Therefore a universal one-Hz traffic ceiling, the full high-RTT envelope and uninterrupted simulation cannot all be promised together. One Hz should describe healthy quiet traffic; loss repair and threatened headroom remain event-driven exceptions. The ADR must select and test those exceptional bounds explicitly.

### ADR042 exact-draft review

The initial `docs/adr/042-event-driven-coordination.md` draft is coherent in its main choices: atomic fresh action cuts, a paired aggregate pulse with dual acknowledgements, age-expanded clock uncertainty, immediate eligible finality and the unchanged execution cap. Its exact packet and lifecycle contract needs four amendments before implementation approval.

1. Specify the exact `settle`/`confirm` tuples, permitted directions/roles, target admission and retained-demand bounds. Coalescing only by alias/target does not bound distinct targets or prevent later requests from continually replacing an earlier attainable target. Prefer one earliest unsatisfied target per peer/origin, preserving it until serviced; later requests must not postpone it. Available current progress can be sent immediately, with the retained target completed when the origin's qualified clock reaches it. Keep retry deadlines independent of duplicates/newer targets and retire satisfied requests on verified finality.
2. Define exactly when each endpoint enables pulse mode and when bootstrap probes stop, including the initially asymmetric case. A local installed segment plus a validated current-association pulse can establish explicit local/remote participation, but the draft must say so; the tuple currently contains no separate mode acknowledgement. Bound the faster 100 ms qualification phase separately from healthy 250 ms retry behavior and preserve the probe escape path throughout transition.
3. Correlate each nested clock nonce with that peer's pending heartbeat attempt. Request clock fields belong only to the follower-to-coordinator direction; response fields belong only to its matching coordinator reply, and non-coordinator pairs must carry null clock fields. A generic pending clock nonce alone is weaker than the promised fresh-attempt RTT contract. Validate the complete closed aggregate shape and slot/role permissions before applying receipts/cuts or refreshing health.
4. Replace descriptive origin-silence and urgent-traffic rules with numeric deadlines, queue sizes and retry/rate caps. In particular, distinguish initial progress grace from the steady pulse-derived deadline, cap urgent certificate/confirmation bursts, and select either a concrete conservative earlier-demand algorithm or the fixed 24-tick profile with explicit hard-cap failure. The fixed profile is acceptable as a bounded operating choice; an unspecified instruction to demand earlier cannot serve as implemented latency protection.

These are implementability and lifecycle amendments, not a rejection of the event-driven direction. Source implementation remains pending review of the amended draft. No source edits, tests, builds or browser runs were performed in this design-review step.

The amended ADR042 closes these four architecture findings and is approved for implementation of its stated bounded profile. It now defines exact demand tuples and roles, one earliest retained target per peer/origin, unchanged demand deadlines, pulse activation/remote-proof flags, correlated heartbeat/clock nonces, clock aging, numeric progress and urgent-traffic limits, and the fixed 24-tick demand/40-tick failure choice with adaptive timing explicitly deferred.

Use two explicit failure-clock interpretations in the implementation and its tests: expiration of the 1500 ms demand must produce visible bounded synchronization failure, not silently discard/recreate the request and restart its deadline; initial origin progress is measured from the observing coordinator's own clock qualification, since remote qualification is not independently observable. Preserve these statements in the ADR prose. Required focused evidence includes responder-side health across ordinary one-second pulses, asymmetric activation and lost responder acknowledgement, unchanged demand expiry across duplicates/newer targets, and quiet finality without a second one-second publication phase. This approves implementation work only; resulting source, impairment behavior, browser integration and measured traffic acceptance remain pending.

### ADR042 heartbeat foundation implementation review

Inspected `src/online/direct-heartbeat.ts` and independently ran `node --import tsx --test tests/direct-heartbeat.test.ts`: ten passed, zero failed or skipped. The helper correctly separates request/reply election, scopes nested clock nonces, validates closed shapes before application, allocates fresh retry IDs, bounds retained attempts and requires explicit acknowledgements for responder health. These tests exercise queued injected delivery; they do not yet exercise a genuinely synchronous paired send.

One lifecycle blocker remains. External `body()` and `accept()` callbacks can synchronously stop or replace the current scope, but the helper continues after those callbacks without rechecking its stopped state. A public-API reproduction where `body()` calls `stop()` still sends one packet and recreates one pending entry; an `accept()` callback that calls `stop()` still returns `accepted` with `acknowledged: true` and refreshes `lastConfirmedAt`. Recheck scope liveness after these external callbacks and before installing state, emitting bytes or reporting new health evidence. Add both stop-during-callback regressions and an actual synchronous pair-send test.

Also resolve the reply-age contract explicitly: the helper currently applies the 500 ms clock RTT rejection to every reply, including non-coordinator pairs carrying no clock. ADR042 distinguishes the clock sample limit from the 2.5-second pulse evidence window. Separate non-clock liveness admission from clock sampling, or obtain an explicit reviewed all-pair RTT restriction. A slower peer-to-peer route must not acquire an undocumented clock restriction merely because both facilities share a tuple. Foundation approval is pending these corrections; runtime cadence and activation are unchanged in this review scope.

The first correction adds post-`body`/`accept` stop checks and separates the 500 ms clock RTT limit from non-clock evidence. Independently reran the heartbeat tests: 13 passed, including an actual synchronous request/reply pair and the two original stop regressions. Complete the same lifecycle fence after responder reply emission: its `body`/`send` callback may stop the helper, but outer `receive(request)` currently still returns `accepted`. That return must not enable adapter remote-pulse proof for a replaced scope. Add responder-body/send-stop coverage before closing this finding.

### ADR042 clock foundation review

Inspected the 2500 ms clock freshness, age-grown sample selection/discrepancy intervals, preserved uncertainty and external nonce support. Independently ran `node --import tsx --test tests/direct-origin-clock.test.ts`: 17 passed, zero failed or skipped. The external nonce and retained-aging behavior are sound within this scope.

One uncertainty correction is required. The added drift radius starts at sample receipt and omits drift during the exchange itself. Under the declared 500 ppm relative drift assumption, a request/reply produced immediately at coordinator tick 100 with all 500 ms of RTT on the return path yields follower estimate 105 while the coordinator has reached 110.005. The current helper reports `ready` with radius 5 despite actual error 5.005. This public-API reproduction was independently run. Include conservative drift across RTT plus post-receipt age, such as `rtt / 100 + (rtt + age) * drift`, and cover asymmetric in-flight drift. An exact 500 ms sample then has already exhausted its five-tick margin on arrival. Clock foundation approval remains pending this correction; no runtime cutover or browser evidence is implied.

### ADR042 foundation correction disposition

Approve the inspected heartbeat and clock foundation corrections. Heartbeat emission/reception now fences callback-driven stop after body construction, application acceptance, send and responder reply emission; stop clears current confirmation and pending attempts. Non-clock reply evidence uses its reviewed 2.5-second window while coordinator clock samples retain the 500 ms admission limit. Clock uncertainty includes drift during the entire RTT and subsequent age, and the asymmetric 500 ms/500 ppm regression confirms the actual remote tick lies inside the reported interval while origination remains unavailable.

Independently ran `node --import tsx --test tests/direct-heartbeat.test.ts tests/direct-origin-clock.test.ts tests/direct-segment.test.ts`: 41 passed, zero failed or skipped (14 heartbeat, 18 origin/clock, nine segment). Coverage includes both responder stop boundaries, synchronous paired sends, retained uncertainty and external nonces. The revised coordinator-loss test appropriately distinguishes the reviewed longer clock freshness from the unchanged 40-tick world limit: immediate peer action remains usable initially, then the world pauses explicitly without exceeding that limit. No threshold was widened to preserve execution. There are no remaining findings in this foundation scope. Aggregate application, demand/finality orchestration and browser transport mode cutover are not yet wired; their implementation and traffic acceptance remain separate review work.

### ADR042 candidate and transport integration boundary review

Initial inspection of `RollbackWorld.prepareProgress` and `DirectTickClock.prepare` finds their candidate copies isolated along the current mutation paths. World cut/finality processing replaces stream/candidate maps and reconstructs replay state; clock acceptance copies pending/sample containers before mutation. Add direct aggregate-rejection regressions: an earlier valid cut followed by a conflicting cut/certificate must preserve the original world hash, cuts, finality, pending certificates and events; rejecting another aggregate field after clock preparation must preserve the original nonce and uncertainty.

The transport pulse-proof API needs a stronger association fence. `acceptPulse(id, segment, acknowledged)` reacquires the current link, whereas the validated bytes arrived on a particular captured link. An alias and current member are not themselves the identity of that RTC association. Carry a captured association guard/token, or return proof through the fast-message callback and apply it to the captured link only after rechecking that it is still current. Cover same-service-connection replacement during application processing, as the existing ordinary link-callback tests do. Also reject proof/activation on drained or closed required channels; the initial `activatePulse` and `acceptPulse` methods check binding alone.

The new health profile and aggregate ingress policy look consistent by inspection: changing 600 ms to 2500 ms freshness first expires old evidence rather than manufacturing a new acknowledgement, and the aggregate spends one reserved control allowance after nested ownership checks. Required tests must establish fresh/expired profile transitions, idempotent mode changes, two fresh acknowledgements after reset, exact aggregate token consumption and unauthorized nested flows. Independently ran the existing direct actions, ingress, origin/clock and online link-health suites: 53 passed, zero failed or skipped. At this point those suites do not yet contain the new candidate/profile/aggregate regressions, so their success is regression evidence rather than full acceptance of these added boundaries. Finality/demand orchestration was deliberately not reviewed while it remained under refinement; no browser or build was run.

The revised adapter now applies returned application proof only to the captured link, action channel and binding, using captured pre-callback activation and a fresh authority check. The production `isCurrentPulseCallback` predicate rejects replacement, closed or drained lanes; activation also requires the current bound-ready predicate. The new candidate-isolation, clock-nonce, aggregate-token, health-profile and same-service/alias replacement regressions close the preceding boundary findings. Approve those foundational boundaries in their tested scope.

### ADR042 segment orchestration inspection

The first complete orchestration inspection found three corrections requiring regression evidence:

1. Queuing an unreliable pulse containing finality deleted its pending reliable certificate. A dropped pulse could therefore suppress prompt certificate delivery and defer progress to another one-second phase. Retire reliable pending finality only after reliable enqueue, or after a validated delivery acknowledgement for the exact pulse carrying that certificate.
2. An older piggyback receipt rejected an entire otherwise valid heartbeat when a newer standalone receipt had already advanced the same queue. Such reordering is normal on the unreliable channel. Treat an authorized stale contiguous receipt as a no-op while still rejecting wrong scope or sequence beyond issued input, and verify fresh clock/progress evidence survives this ordering atomically.
3. Pending `GameEvent` detection does not cover every phase transition: the shared reducer changes countdown to playing without emitting an event. Detect an unfinalized phase change explicitly, without changing LAN event semantics, and test a countdown boundary offset from ordinary certificate ticks so public finalized phase updates promptly.

Independently ran direct actions, origin/clock, ingress, online link-health, link-callback and segment suites: 73 passed, zero failed or skipped. This includes candidate/proof boundaries, failed confirmation delivery expiring despite repeated/newer targets, six-world loss/reordering, and quiet ten-second traffic reporting exactly 300 paired pulses plus 50 certificates with no other recorded message classes. The reported uploads (coordinator 611 B/s, players 101 B/s, display 79 B/s) are deterministic application-payload observations, not browser or wire measurements. Source fixes for the first two orchestration findings were underway during review; their dedicated regression disposition and the phase-transition correction remain pending. No browser or build was run.

### Pulse deactivation during lifecycle preparation

Approve explicit alias-scoped pulse deactivation when runtime `freeze()` ends the segment. Otherwise an adapter remains in pulse mode while its application has stopped producing pulses, allowing a slow but bounded checkpoint transfer to outlive path freshness. Deactivation must resume bounded bootstrap health without creating new acknowledgement evidence or extending preparation/episode deadlines.

Do not interpret any incoming ordinary legacy probe as a pause request. A delayed probe from before activation could clear remote participation, causing both endpoints to restart legacy probes that repeatedly clear each other and restore unwanted steady traffic. Use an explicit authenticated pause marker or a monotonic mode generation. Because freeze permanently ends the segment, a one-way paused latch for that alias is sufficient; delayed old pulses cannot reactivate it, and a new binding resets it. One compact option is a distinct pause-probe `[1, alias, 12, probeId]`, acknowledged through the existing pong and retried on the existing bounded 200 ms bootstrap schedule. Ordinary probes then have no mode-changing meaning. Record the selected exact encoding/latch rules in ADR042 and test loss, duplicate/stale pause markers, delayed ordinary probes, and slow checkpoint preparation before implementation approval of the chosen wire amendment.

The three preceding segment orchestration corrections are now inspected and approved. Unreliable pulse enqueue no longer retires reliable finality; `prepareReceipt` preserves a newer acknowledged prefix when an older authorized receipt is piggybacked; and rollback candidates retain bounded phase-change ticks separately from emitted game events, reconstructing/trimming/counting them with the retained history. This covers countdown confirmation without changing LAN event semantics.

Independently ran `node --import tsx --test tests/direct-segment.test.ts tests/direct-actions.test.ts`: 42 passed, zero failed or skipped. New serialized regressions cover a stale receipt alongside a fresh clock sample, a lost certificate pulse while reliable enqueue retries, and publicly confirmed countdown completion between ordinary certificate ticks. Quiet traffic remains 300 pulses plus 50 certificates over ten seconds in the deterministic fixture. These close the three reported orchestration findings; the separate explicit lifecycle pulse-deactivation amendment remains pending its exact contract and implementation review.

The selected pause-probe amendment is now recorded in ADR042 and implemented with `LinkPulseMode`. Inspection confirms irreversible local/remote pause per binding, explicit kind-12 pause and kind-9 acknowledgement, ordinary probes without mode-changing side effects, rejection of delayed application pulses after pause, and new-binding-only reset. Runtime freeze explicitly deactivates the installed segment alias before stopping it. Independently ran online link-health, link-callback and direct-runtime tests: 36 passed, zero failed or skipped, including mode-latch and scheduled-settings/deactivation/new-alias activation regressions.

Complete one shared probe-send guard before closing this transition review: `sendFastProbe` itself does not check stopped/hidden state or the reliable lane's live undrained state. Periodic pause probes inherit outer checks, but a deferred pause-probe reply can cross visibility or closing changes before it runs. Apply the same health-independent lifecycle and required-channel guards at the actual send boundary and test deferred reply after hide/drain. Do not require healthy gameplay permission for these recovery probes.

Document an operating-scope exception as well: an intentionally coordinator-null shared lobby has no installed segment and intentionally no five-second simulation preparation deadline, so its legacy health probes can continue indefinitely. The ADR's blanket five-second bootstrap description needs this exception, or a separate reviewed idle-room transport profile. The measured one-second quiet cadence currently describes active segment participation, not every connected room waiting for a TV.

### Cross-alias bootstrap amendment

Approve the proposed dual association-scoped bootstrap for paused bindings. The parent's full-match `direct-traffic-heartbeat-01` failure exposed the cross-alias dependency: one participant binds the next alias before checkpoint delivery, while a paused peer still emits only the previous alias's compact probes because its old `remoteConfirmed` is true. Those probes are correctly rejected, but lost path health then blocks the reliable header acknowledgement required to complete the transition.

While either local or remote pause is latched, send the existing authenticated reliable bootstrap probe alongside compact pause/ordinary probes regardless of the old binding's `remoteConfirmed`. Matching reliable replies remain association-scoped and use existing bounded sends, 200 ms pacing and phase/episode deadlines. A fresh binding resumes ordinary alias-confirmation logic; healthy active pulse participation still retires standalone probes. This repairs the circular startup dependency without treating queued metadata as application acceptance or weakening gameplay health gates. Add regression evidence with the two peers on adjacent aliases and delayed plan/header progression, verifying header acknowledgement and chunk transfer resume. Implementation verification remains pending. The intentionally idle waiting-for-TV bootstrap exception is an appropriate documented scope limitation, separate from active-game one-second traffic claims.

### Final pause/bootstrap boundary disposition

Approve the inspected boundary corrections. `needsAssociationProbe` requires authenticated association probes for either pause latch regardless of old alias confirmation, and the adapter uses it before suppressing JSON bootstrap traffic. `permitsFastControl` rechecks visibility, stopped state and both live undrained channels at the actual pulse/probe send, while correctly leaving fast health evidence independent of reliable backlog. Deferred reliable bootstrap sends also recheck stopped/hidden state and fast-lane drain. ADR042 now explicitly distinguishes active preparation deadlines and one-second active-segment cadence from indefinitely waiting shared-TV lobbies.

Independently ran `node --import tsx --test tests/online-link-health.test.ts tests/link-send-gate.test.ts tests/link-callback.test.ts`: 15 passed, zero failed or skipped. The new policy-level serialized probe regression covers local and remote pause with mismatched aliases, rejects the compact alias and obtains two fresh association acknowledgements before considering metadata send eligible. The deferred-send regression covers hide, stop, closing and drain, including permitted fast control during reliable backlog. These verify the production helper boundaries; the probe test does not itself execute a native checkpoint transfer. There are no remaining findings in this pause/bootstrap boundary scope. The parent's actual mixed-browser full-match retry and complete acceptance remain separate evidence; no browser or build was run by this reviewer.


### Application impairment and traffic measurement review

Inspected the seeded send queue, native traffic fixture and reporting script. The model applies shared per-sender serialization bandwidth and delay/jitter to both RTC lanes, preserves each reliable channel's FIFO order, and drops/reorders only fast messages. Its packet/byte bounds are explicit and overflow fails qualification. Queued binary data is copied. Native accepted-send and delivered-message counters are correctly distinct from synthetic enqueue attempts; the reported payload bytes do not include SCTP/DTLS/IP overhead or emulate retransmission. Fast loss is sampled before bandwidth serialization, so dropped synthetic packets do not consume the simulated bandwidth budget. This is a declared application-queue profile, not a claim about physical mobile network behavior.

Three initial measurement defects were identified and corrected during inspection: synthetic queued bytes now contribute to each channel's `bufferedAmount`, preventing checkpoint send gates from overlooking fixture backlog; impairment remains enabled after the counting window, preventing new reliable sends from bypassing queued older messages; and sticky runtime `lastFault` plus bounded status/diagnostic traces prevent successful automatic recovery from hiding a failed healthy-play run. Queued bytes at window end and cancelled bytes at stop are reported rather than silently described as delivered. Independently ran `node --import tsx --test tests/direct-network-queue.test.ts`: four passed, zero failed or skipped, including FIFO/copying, shared bandwidth and fast-only loss, resource bounds, and per-channel buffered-byte accounting.

Before treating the harness as final acceptance evidence, capture console errors as well as uncaught page errors, since the historical WebKit native-send symptom can be console-only. Also associate source hashes with the exact bytes esbuild consumed, or verify input stability around bundling: reading sources only after the build can misattribute a bundle during concurrent edits. Capture pre-build revision/dirty metadata rather than labeling the result only with repository state at the end. The bundle digest itself already identifies the executable artifact. Browser profile runs remain the parent's separate work; no browser or build was run by this reviewer.

The regional transition failure additionally exposed redundant acknowledged checkpoint headers: full `directPrepare` metadata repeats until readiness even after `directHeader` confirms receipt. Approve stopping header retries on the current member's header acknowledgement, with an exact two-key ACK shape and existing revision/member/connection guards. Chunk progress, ready and applied acknowledgements remain separate. Required regression covers a lost initial header ACK and retransmission, suppressed header traffic while acknowledged checkpoint chunks remain backpressured, and eventual readiness/activation after release. This paragraph approves the bounded design correction; resulting production source and tests remain to be reviewed.

### Presentation follow-up requirements

The brief's adaptive 50–100 ms history is a proposed starting range requiring its own design and rendered measurements. Use recent high-percentile action lateness plus clock/interpolation margin, increase promptly and reduce slowly, retaining useful jitter evidence during sparse input. Related remote riders, trails, projectiles, pickups and effects need one coherent presentation time. Immediate local response must be evaluated together with collision visibility when remote trail geometry is displayed from an older tick. History must be bounded and invalidated or rebuilt after rollback and lifecycle changes, without changing simulation collision ticks, introducing renderer physics or replaying committed effects. Qualify local rendered response, correction tails, frame cost and collision clarity together. The current traffic fixture has no renderer and cannot establish those presentation outcomes.


### Header acknowledgement and measurement correction disposition

Approve the inspected header implementation. Retransmission now stops at the current member's validated header ACK, independently of chunk completion, readiness and activation. The ACK requires exactly the type and current revision fields; existing plan and connection membership guards remain. Independently ran `node --import tsx --test tests/direct-runtime.test.ts tests/direct-network-queue.test.ts`: 32 passed, zero failed or skipped. Regressions cover failed header and ACK enqueues, resumed duplicate acknowledgement, no repeated metadata while chunks are backpressured, stale/malformed ACK rejection, and successful subsequent activation.

The measurement script now captures browser console errors as well as page errors. Its esbuild loader hashes exactly the contents returned to the bundler, asserts every local bundle input has a recorded hash, and captures revision/dirty metadata before building. These close the preceding measurement refinements. The queue's explicit pre-SCTP model and native-accepted payload counters are suitable for the declared exploratory impairment scope. Failed regional profile runs remain failures; these fixture corrections do not establish reliable play under those profiles or public/mobile qualification.

### Checkpoint pacing requirements for the next diagnosis

The next checkpoint pacing change must bound shared sender backlog, not only each RTC pair independently. Account encoded payload size, the prospective send and reliable/fast queued data across links; preserve control/probe scheduling room and fair progress among recipients. Smaller chunks alone leave aggregate delay unbounded by the intended per-link limit. At 256 kbps a 12 KB chunk costs roughly 375 ms of serialization and five such chunks roughly 1.875 seconds, before propagation, which cannot preserve the current 600 ms bootstrap health schedule. Headers and plans consume the same uplink and also require consideration. Native `bufferedAmount` is an observable queue bound, not a guarantee about bytes already in SCTP or the physical network.

Before selecting constants, retain bounded per-class enqueue/native-send timing, aggregate/channel backlog and health-ack timing in the failure evidence. Keep checkpoint offsets advancing only after successful enqueue, retain original setup and recovery-episode deadlines, and visibly reject or fail checkpoint transfers that cannot fit the supported bandwidth/deadline budget. A pacing policy should be tested with five competing recipients and simultaneous health/control traffic, showing both progress after drain and bounded safe failure. Reusing an already validated local base for ordinary transitions remains a separate architecture amendment; it must preserve exact base hash, lifecycle authority and fallback checkpoint validation. No production pacing change or native acceptance is approved by this requirements note.


### Finalized checkpoint reconstruction blocker

Independently reproduced the regional failure's exact world API symptom. The direct reducer can produce a state that the checkpoint reader rejects: a survivor presses a bomb on the tick the round ends, then releases or cancels after the game enters `roundOver`. `stepDirect` clears the active gesture and held bomb flag, but `game.step` returns early outside `playing` and leaves the survivor's `bombChargeStartedTick` intact. The game checkpoint alone remains valid; `readState` correctly rejects its inconsistent charge-without-active-gesture relationship. This is a reducer/checkpoint invariant violation, not evidence of mutable snapshot aliasing.

The finality path compounds it. A non-snapshot `stateAt(tick)` replays into memory without validating a new serialized checkpoint. `tryFinality` can hash and accept that state, save it as the new exact finalized snapshot, and only afterward discover on `finalizedState()` that the saved bytes are unreadable. Independent public-API reproduction: construct a validated two-player playing tick-60 base, eliminate the other player through the shared public operation, ingest survivor actions `[1,61,1,1]` and `[2,62,2,1,null]` with cut `[64,2]`, certify the other stream through 64, and advance to 64. `proposeFinality(64)` returns a certificate, `finalize` reports accepted and sets finalized tick 64, but `finalizedState()` returns undefined. A preceding direct-state reproduction showed game-only decode succeeds while direct bootstrap decode fails after the release.

Required correction: preserve the direct gesture/charge invariant when a release or cancellation occurs outside active play, without silently changing LAN semantics. Before installing newly reconstructed finality, verify its checkpoint can be restored; handle reconstruction rejection explicitly and retain the prior healthy finalized checkpoint instead of allowing non-null assertions to leak an undefined state. Regressions should include round-over and match-over release/cancellation, late delivery through rollback, and finality on a tick not divisible by the five-tick snapshot interval. This is a correctness blocker independent of the still-open checkpoint pacing work; no native run was performed by this reviewer.


### Finalized checkpoint correction disposition

Approve the inspected `fuse-direct-3` correction. After each direct reducer step outside `playing`, charge and target state are cleared while held controls and gesture stream identities remain intact. The shared LAN reducer is unchanged. Finality now rejects missing reconstruction explicitly and requires the newly packed checkpoint to decode to the exact certified hash before committing the finalized tick, trimming history or returning events. Independently ran direct actions and direct segment suites: 43 passed, zero failed or skipped. The new regression covers eight round-over/match-over, release/cancel and immediate/late rollback combinations at non-snapshot finalized tick 64. This closes the reproduced unreadable-finalized-checkpoint blocker; native impairment reliability and checkpoint pacing remain separate open work.

### Proposed aggregate checkpoint pacing review

The proposed operating policy is coherent with unchanged recovery deadlines: 2,000-byte chunk payloads, at most one successfully queued chunk per runtime scheduler tick, per-recipient 50 ms eligibility, fair recipient rotation, a 6,000-byte aggregate bulk admission cap, and a 12,000-byte aggregate management admission cap. Both caps must include the prospective encoded envelope and all current RTC reliable/fast queue bytes. Compact reliable finality/demand sends must also use the management cap or have an explicitly bounded and justified exception; otherwise their bursts bypass the intended shared backlog limit. Health probes retain their independent per-lane recovery gates and must not require gameplay health.

Skip recipients that are ineligible or backpressured, rotate after successful enqueue, and recheck the outgoing preparation/plan after synchronous transport callbacks before advancing offsets. Preserve action-lane-idle bulk gating and five-second preparation/fifteen-second episode bounds. These are operating limits for measurement, not a guarantee that 375 ms of queued serialization plus RTT and scheduling fits a 600 ms health profile. Record exact constants and exceptions in the ADR before production implementation, then test five-recipient fairness, prospective-byte boundaries, control/probe progress and lifecycle replacement during send.


### Aggregate checkpoint pacing implementation inspection

The new transport code uses complete prospective encoded bytes plus every reliable/fast RTC lane in aggregate admission. Management and compact reliable controls use the 12,000-byte cap; checkpoint sends use 6,000 and retain destination fast-idle gating. Runtime chunks are 2,000 bytes, one successful enqueue per scheduler tick, with per-recipient 50 ms eligibility, ineligible-peer skipping and rotation after success. ADR041 records the exact policy and probe exception. This matches the reviewed pacing scope by inspection.

One callback lifecycle guard needs correction before disposition. Post-header and post-chunk checks compare transfer and plan object identities, but `stop`, terminal shutdown and a required-lobby recovery latch can preserve both objects. A synchronous callback can therefore stop the runtime while its current scheduler callback still advances the old transfer offset or proceeds toward activation. Recheck stopped/recovery status and current authority/plan membership alongside identities. Add a public callback stop/recovery regression, complementing authority replacement (which already clears the old objects). Focused tests have not yet been independently rerun while native qualification is active. The retained regional-05 artifact reports failure requiring recovery with no browser errors; pacing source inspection does not promote it to a passing profile.


### Synthetic unsent-byte model refinement

Approve separating sender serialization backlog from propagation-delayed fixture retention. The initial synthetic `bufferedAmount` counted bytes until delayed native delivery, so propagation time occupied a sender-unsent budget and could artificially throttle bulk transfers under the new 6 KB cap. Keep every delayed entry within the existing queued-byte/memory bound, but count it toward synthetic buffered bytes only until its shared-bandwidth serialization completion. Preserve a separate completion timestamp from the jittered and reliable-FIFO-adjusted delivery due time; release buffered accounting exactly once. Report current/maximum buffered bytes separately from retained queued bytes.

Tests must establish shared cross-channel serialization pressure, a propagation-pending interval with zero synthetic unsent bytes, FIFO despite jitter, and correct clearing/closed-send accounting without negative or double decrements. This remains a pre-SCTP application impairment approximation, without physical congestion-window or acknowledgement modeling. Retain regional-05 as a failed run with the earlier propagation-as-buffer limitation; its low checkpoint throughput cannot directly establish production behavior. This paragraph approves the model amendment; implementation verification remains pending.


### Final pacing boundary source disposition

The inspected post-header/chunk guards now include stopped state, required-lobby recovery, current authority and current membership, as well as exact transfer/plan identity. This closes the reported stale-transfer continuation path by inspection. The synthetic queue now tracks `serializedAt` separately from delayed delivery, releases each entry's unsent-byte accounting once, retains propagation entries under the memory bound, and reports buffered versus retained maxima separately. The updated queue regression covers shared serialization, propagation-only retention, repeated pumps and clearing. No further source blocker was found in these bounded changes.

Strengthen the new end-callback regression before calling it proof of the new guard: ending during the first chunk can already yield one chunk and a cancelled scheduler under the preceding one-success-per-tick logic. Stop during header delivery and assert no subsequent same-tick recipient sends, or stop on the final readiness-completing chunk and assert no activation. Independent focused rerun remains pending while regional-06 executes; the parent's reported unit results are not represented here as reviewer-run evidence. Native regional qualification remains separate.


### Completed bounded pacing and fixture review

Independently ran `node --import tsx --test tests/direct-runtime.test.ts tests/link-send-gate.test.ts tests/direct-network-queue.test.ts` on the finished corrections: 40 passed, zero failed or skipped. The strengthened callback regression ends the creator during its first preparation-header send and observes no further recipient sends in that same scheduler tick, closing the preceding test-evidence note. The suite also verifies prospective aggregate encoded-byte limits, control headroom, recipient rotation and offsets, persistent-backpressure deadlines, and serialization-versus-propagation queue accounting. Approve these bounded production fixes and fixture corrections in their tested scope; no findings remain within this fix review.

Overall regional qualification remains blocked. The parent reports regional-06 still fails ordinary round transition preparation within the unchanged five-second bound, without browser exceptions or fixture overflow; reduced queue residence is not successful gameplay recovery. Preserve all regional attempts and each fixture model's limitations. Further transfer diagnosis or separately reviewed validated-base reuse is required before claiming the profile works reliably. This disposition does not approve deployment, extended network tolerance or physical-phone behavior.


### ADR043 validated-base reuse design review

The proposed fast path is appropriate: an exact retained finalized state plus authenticated deterministic management operations can produce the same replacement bootstrap as full transfer, without changing simulation authority, allocating another history or relaxing activation. Selective payload requirements preserve the existing checkpoint fallback for genuinely missing bases. Design approval is conditional on these exact protocol clarifications before implementation:

1. Specify closed reference and `directHeader` ACK shapes, field names, integer/hash types, and the boolean payload requirement. Define whether the 4,096-byte bound covers the entire encoded reference. A full-view no-payload ACK is valid only for an offered reference; controller no-payload ACK remains valid without one. State the exact rejection behavior for conflicting per-peer requirements while preserving already admitted progress.
2. Define retained world provenance precisely. Keep the original installed plan/world binding; verify the exact prior alias, room/epoch and current source/local connection identities against that binding. The new plan cannot relabel a retained world. Clarify which connection replacement invalidates reuse: an unrelated new or departed roster member should not automatically disqualify every unchanged peer, while a replaced source or local connection must. The source may be the previous coordinator rather than the original source that prepared that older segment.
3. When a reference is offered, validate the transferred fallback's base alias/tick/hash and canonical operation list against it too. A cache miss must not bypass the reference proof. Shared derivation and identical output/header checks remain required on both paths. Distinguish absent/out-of-scope/different retained state (ordinary miss) from an authenticated exact-scope proof or output contradiction (corruption).
4. An ahead-local finalized tick is a reuse miss, but fallback still cannot downgrade the retained fence; document the resulting safe rejection rather than promising that every miss can recover from the offered payload. Keep phase/episode deadlines unchanged.
5. Construct one immutable preparation/reference choice for all recipients. Omit an oversized reference based on serialized envelope capacity, never transient queue occupancy or peer-specific admission; otherwise retries can produce conflicting same-scope headers. The complete prospective actual envelope still passes the existing aggregate send gate when it is eventually enqueued.

The proposed acceptance list otherwise covers the key observable boundaries. Add explicit coverage for unrelated membership additions preserving other peers' eligibility, prior coordinator/source distinction, fallback/reference mismatch, and an ahead finalized fence that cannot be bypassed by opting into payload. Record reuse hits/misses and transition payload bytes separately in native evidence. The existing passing local full match and failed regional-06 remain evidence for the current implementation, not evidence for this unimplemented proposal. No source, browser or tests were run for this design review.


### ADR043 amended design approval

Approve the amended ADR043 for implementation. It now defines the exact four-element, 4,096-byte encoded reference and three-key boolean payload ACK; retains original world/plan provenance and current local/source connection checks without invalidating unrelated peers; validates fallback base and operations against the offered reference; preserves ahead/equal-hash finalized fences; and fixes one immutable header using worst-case recipient envelope size. Conflicting current-scope payload requirements fail the preparation without changing admitted transfer mode or progress. These amendments close all preceding design findings.

Implementation must retain shared derivation/output validation, controller lightweight behavior, selective bounded fallback, and unchanged ready/applied/timeout semantics, with the listed serialized and native regressions. This approves the architecture for development only. The existing rules-3 local/UI evidence and regional failures do not qualify the proposed rules-4 implementation; resulting source review and regional acceptance remain outstanding. No code or tests changed during this design review.


### ADR044 adaptive presentation design review

Approve the amended ADR044 for implementation. Initial inspection found two concrete pitfalls: existing `interpolateWorld` publishes the newer frame immediately across phase/round changes, and temporary rollback/finality reconstruction could accidentally mutate or discard the installed four-frame history. The amended contract now holds the older raw frame until the discontinuity boundary, uses a direct-specific presentation path, preserves unaffected history while rebuilding the replay suffix, and isolates temporary replay maps and mutable game fields.

The final amendment also makes lateness sampling occur only from newly contiguous actions after complete world admission using one captured clock reading, overlays finalized outcomes after raw interpolation, and supplies the local trail extension explicitly for both Canvas and Phaser. That avoids relying on `trailTip`'s current-tick condition when the overall scene uses a delayed tick. History remains bounded inside the existing byte ceiling, aliases reset presentation state, and rendering neither runs physics nor publishes committed effects. No remaining design blocker was found within this short-history scope.

Implementation review must verify the listed replay, sampling, discontinuity, local-preview and lifecycle regressions. The specified rendering fixture remains necessary to evaluate response and corrections; neither this design review nor existing simulation-only traffic results establish rendered performance. LAN/solo interpolation and the existing simulation/finality contracts remain unchanged. No production code, tests or browser runs were performed during this design review.


### ADR044 first implementation review

The four-frame history reconstructs through independent candidate maps, retains pre-checkpoint frames appropriately, counts encoded frame bytes, and survives finality trimming. Newly contiguous remote actions supply samples once, including buffered records released by gap repair; raw interpolation precedes finalized outcome overlay. Three concrete corrections remain before implementation approval:

1. `renderSnapshot` still calls adaptive presentation when the clock cannot advance. Only local fractional preview is disabled, so unqualified clocks can move remote playback through retained history or change the delay. Preserve the last presented scene, including its local overlay, while frozen/unqualified/faulted. Test repeated renders across qualification loss and recovery without simulation/network side effects.
2. Shell interpolation inherits an insufficient equal-velocity test. A shared `advanceShell` reproduction starting x=100, vx=450 within bounds 95..105 traverses 100→105→95→102.5 in one tick and ends with vx=450. The current direct interpolator draws x=101.25 at half tick, while the actual bounced path is x=98.75. Require compatible shell type/velocity and displacement consistent with straight elapsed motion, otherwise hold the older pose until the next tick. Keep LAN interpolation unchanged.
3. Pending-certificate rejection can mutate installed history. Independent public world API reproduction: validated tick-60 base, advance64, certify the other origin through64, queue a waiting bad-hash finality64 requiring origin0 sequence1, then admit origin0 late turn `[1,61,0,1]` with cut `[64,1]`. `receive` returns invalid/corrupt, but history changes because streams/replay candidate install before `tryFinality` rejects. Compose the admission/finality work atomically or restore the prior candidate/streams on rejection, retaining bounded corruption handling; test this path explicitly. This is an existing simulation-admission weakness newly covered by ADR044's atomic presentation contract.

These findings are limited to the approved presentation scope. Two small deterministic public-API reproductions were run; no browser or broad test suite was run during concurrent rendered measurement. Source implementation approval remains pending their corrections.


### ADR044 correction inspection

The shell fix now restricts interpolation to velocity/type-compatible straight displacement, holding the earlier shell pose for the reproduced multiple-bounce case. Admission and advancement restore prior simulation/history candidates when pending finality rejects or overflows. The modified finality regression correctly asserts that a rejected progress cut was not installed, then retries it before expecting valid finality; this strengthens atomicity while preserving recovery coverage. These two corrections are sound by inspection.

Caching the full rendered scene fixes continued playback under an unqualified clock, but introduces one scope reset issue: the cache is cleared only by segment activation. Explicit coordinator-null shared lobbies and fresh-lobby recovery can replace the public catalog while leaving no simulator to activate, and `renderSnapshot` then prefers the previous game's cached scene indefinitely. Clear/rescope this cache at explicit world-discard/idle/fresh-lobby boundaries while retaining it during a same-scope freeze. Add a rendered playing→shared-without-TV→catalog-update→TV-join regression. Approval of the completed freeze correction remains pending that lifecycle fix. No concurrent browser or full test run was performed during the parent's response measurement.


### ADR044 final cache reset verification

Inspection confirms explicit world-discard boundaries now clear the rendered cache, and no-world/controller rendering returns the current catalog directly. A narrow independent run of the four correction regressions passed shell bounce compatibility, pending-finality history atomicity and unqualified-scene freezing, but the new idle-catalog scenario failed its rename expectation (`p1` rather than `Renamed rider`). The cache source fix appears sound; verify that the chosen catalog mutation is actually accepted and updates the public view before asserting its rendering. Output is retained locally at `/private/tmp/adr044-review-corrections.log`. Final test-evidence disposition remains pending that corrected scenario, with no additional source blocker identified.


### ADR044 bounded implementation approval

Approve the corrected ADR044 implementation within the reviewed scope. The final cache regression now uses a supported creator bot-add, explicitly verifies successful command acceptance and the changed published idle catalog, compares rendering against that catalog, then activates a TV without stale scene reuse. Independently reran that exact test: one passed, zero failed or skipped. Together with the preceding three independently passing correction regressions, this closes shell motion, rejected-admission atomicity, unqualified presentation freezing and cache lifecycle findings.

No concrete source blocker remains in this bounded presentation review. Rendered response/correction measurements, complete checks and regional/extended qualification retain their separate evidence requirements; this approval does not infer those outcomes or authorize release.


### ADR043 bounded implementation approval at 2024f5d

Approve the inspected state-reuse implementation at `2024f5db2bbdc98fa154ecd62c0708991b28db87` within ADR043's scope. Reference and ACK validators enforce exact tuple/object fields, numeric/hash rules, operation bounds and encoded reference size. Header selection accounts for the largest complete recipient envelope with a maximum-width message ID. Local reuse and transferred fallback share base proof, canonical operation, neutralization, roundtrip, finalized-fence and resulting-header validation. Installed plan provenance remains separate from newly adopted plans; source/local connection or authority changes prevent reuse while unrelated membership changes do not. Duplicate headers preserve the chosen payload requirement, contradictory ACKs fail safely, and only acknowledged peers requiring payload receive paced chunks. No new correctness blocker was found.

Independently ran `node --import tsx --test tests/direct-room-state.test.ts tests/direct-runtime.test.ts`: 49 passed, zero failed or skipped; completed output retained at `/private/tmp/adr043-independent-final.log`. Evidence includes serialized reference/ACK boundaries, largest-recipient envelope sizing, exact reuse/transfer output, altered proofs, newer finalized fences, scope changes, zero-chunk hits, selective fallback, retry and conflicting modes. The runtime rejection of an unoffered no-payload claim is additionally verified by source inspection; this disposition does not invent a dedicated runtime test for that branch.

This closes the remaining bounded ADR043 implementation review. The current mobile application-impairment run, reported by the parent as failing a round-transition setup deadline without browser errors or overflow, remains a failed profile requiring separate diagnosis. A short passing local/regional fixture is not evidence of mobile, sustained or physical-device reliability, and implementation approval is not release authorization.


### Bounded preparation-header retry pacing amendment

Approve the proposed delivery refinement prompted by the mobile trace's repeated large unacknowledged headers. Send the first immutable preparation header immediately. After a successful reliable enqueue, retry no sooner than 500 ms; after a failed enqueue, retry after 100 ms. Track this per peer separately from activation retry timing. A validated header ACK stops repetition, while duplicate headers still return the same immutable payload requirement. Neither successful enqueue nor a retry is application acceptance or permission to reset five-second preparation/fifteen-second episode deadlines.

Record the exact refinement in ADR043 before implementation. Regression evidence should withhold/delay ACKs, verify successful header enqueues remain at least 500 ms apart, verify 100 ms failed-enqueue retry, and retain independent readiness/activation behavior. This is a bounded metadata delivery policy, not approval to infer missing finality or change exact-base reuse. Its mobile impact remains to be measured; no source or test was changed by this design review.


### Preparation-header retry refinement implementation approval

Approve the inspected small runtime change. A distinct per-peer `nextHeader` deadline now permits the first send immediately, schedules 500 ms after successful enqueue and 100 ms after failure, and stops repeating on validated header acknowledgement. Activation retains its independent `lastMeta` timing, and original attempt/episode bounds are unchanged. The updated stale/malformed ACK test waits through the new retry interval without weakening rejection.

Independently ran the new withheld-ACK regression alone: one passed, zero failed or skipped. It proves exact 100 ms failed-enqueue retry, 500 ms spacing while acknowledged delivery is absent, stopped header repetition after a valid ACK, and separate readiness until blocked chunks are released and activation succeeds. Completed output is retained at `/private/tmp/adr043-review-header-retry.log`. No findings remain in this bounded delivery refinement. Mobile-02's outcome remains separate; no browser or build was run by this reviewer.


## ADR045 deterministic round progression design approval

Reviewed the proposed `045-deterministic-round-progression.md` against the current direct reducer, shared `startNextRound`, origin admission, runtime status and presentation boundaries. Approved for bounded implementation: ordinary round advancement is an exact-tick deterministic rule within the existing segment, while explicit management/membership/recovery retains lifecycle barriers. The documented finalized-only local origin reset preserves consumed UI revision and wire/gesture identities, and addresses the otherwise divergent held-control state. Pending settings must be canonical direct state applied identically through reuse and payload derivation; active match format/length stay fixed. Same-scope controller status can advance finalized round/tick, and a speculative new round must render the complete finalized scene until proven. Event identity must retain its originating round.

Implementation verification must cover reset admission failure without declaring a successful reset, sentinel/boundary UI revisions without consuming the next user input, and identical pending-settings clone/bootstrap/hash representation. These follow the existing transaction and serialization contract. No additional design blocker remains. This approval does not establish that the constrained mobile profile passes; both recorded failures and the unchanged qualification criteria remain applicable.


### ADR045 initial implementation review — two boundary corrections required

The direct reducer now advances at the exact existing deadline, clears held controls through the shared lifecycle operation, retains latest gesture identities, and includes pending settings in cloned/hashed/bootstrap state. Committed events retain their original round. The origin reset preserves UI revision and uses ordinary sequenced admission; presentation returns the complete finalized scene when round identities differ and suppresses the current local pose in that case.

Two concrete corrections remain before implementation approval:

1. The new controller `directStatus` ownership check assumes every human slot belongs to that player ID. The existing `ownersFor` contract assigns retained disconnected humans to the coordinator, so a valid playing-phase departure can cause subsequent status rejection on all controllers. Its bot-prefix comparison also fails to prove exact immutable bot identity. Validate exact slot/player identity against the preparation roster and reuse the established ownership contract. Regressions should cover shared-TV status after a retained human departure and reject a substituted bot ID.
2. Applying pending round settings dereferences optional `game.settings` through non-null assertions. Valid shared/direct checkpoints may omit game settings, where existing rules use wins/three defaults. Such a state with pending settings can therefore throw at the next-round deadline. Preserve shared defaults or reject that combination at the validation boundary; cover validated bootstrap through deadline.

This is source review only so far; no browser/build or concurrent qualification test was run by the reviewer. The parent is adding focused regressions and retaining the unchanged network acceptance requirements.


### ADR045 bounded implementation approval

The two reported source issues are closed. Controller status now compares exact slot/player identity with the already validated preparation roster, allowing its approved coordinator-owned disconnected human slots while rejecting bot substitution and round/tick regression. Pending-settings application preserves the shared wins/three defaults when the pure reducer receives a game without explicit settings. Correction to the initial review's scope: `checkpoint.ts` already requires game settings in validated checkpoints, so the absent-settings dereference was a pure-reducer boundary issue, not an admitted-bootstrap crash. The new regression exercises that pure path and validates the resulting explicit-settings bootstrap.

Independently ran eleven focused ADR045 regressions: eleven passed, zero failed/skipped. Coverage includes exact automatic deadline, late/reordered boundary replay, pending-state roundtrip and invalid settings rejection, reuse/fallback equivalence, too-few-connected participants, revision-preserving reset including sentinel/max revisions, coherent finalized scenes, serialized individual/shared-TV consecutive rounds without lifecycle traffic, and retained-departure/bot/status identity boundaries. Completed output: `/private/tmp/adr045-independent-corrections.log`. Source inspection also confirms committed-event round identity is captured before each simulation step and retained until finality.

Approve this bounded ADR045 implementation. The separate mobile-03 countdown recovery/completeness issue reported by the parent remains a qualification failure requiring its own diagnosis; completing three rounds does not erase that recovery or establish full mobile acceptance. This review did not run a browser or build and grants no deployment authorization.


### ADR042 behind-peer progress repair design review

Approve the bounded correction in principle: a receiver's proven finality does not establish that every other simulator has received all origins' cuts. Authenticated, scoped, bounded already-finalized requests may therefore resend existing certificates and cause owners to publish fresh exact cuts, without relaying another origin's completeness or advancing unproven state. Keep the sender's existing demand deadline and hard execution cap.

Before implementation, make the cross-requester pacing explicit: `demandAdmitted` is a per-requester admission bound, not a fanout bound. The already-finalized path must share existing per-owner/self `demandSent` timing and per-requester pending-certificate retry timing with ordinary pending demands, so five simultaneous requesters cannot multiply own-cut/confirm fanout beyond one per destination per 250 ms. Duplicate requests cannot reset those timestamps. The current shared incoming admission map is appropriate because the role rules permit settle only at the coordinator and confirm only from the coordinator at an owner. Authenticate roles/scope before spending that admission.

At the segment base there need not be any certificate to resend. The proposed strict `target > baseTick` requirement means `localFinality == baseTick` can never enter the already-finalized repair branch; use the existing pending-demand path and never synthesize a base certificate. This is design review only; resulting source, failure retry and rate regressions remain to be inspected.


### Behind-peer repair implementation review — certificate pacing clarification required

The new already-finalized request handling correctly retains scope/role/future/base admission, shares `confirmOrigins` pacing across requesters and pending/repair paths, and sends each owner’s own fresh cuts. The new tests exercise an already-finalized request, selectively missing quiet pulses, and simultaneous requester coalescing with role/alias/base rejection.

One unintended broader behavior remains: persistent `finalityAttempts` timestamps are now reused for every newly proposed certificate, including after the previous certificate was successfully sent. That delays genuinely new urgent frontiers until 250 ms, whereas the existing rule permits a new urgent certificate after 50 ms and applies 250 ms to failed/coalesced retries. Preserve the new duplicate-repair bound while retaining fresh-frontier timing (for example, record attempted frontier and success along with timestamp). Add a timing regression distinguishing a new frontier after successful send from the same certificate’s repair and a failed send retry. Otherwise this needs an explicit broader cadence amendment and qualification; it should not enter as an incidental behind-peer repair change. No browser/build was run by the reviewer.


### Behind-peer progress repair implementation approval

Approve the corrected bounded implementation. Persistent per-recipient certificate attempt state now records frontier, time and enqueue outcome: duplicate/older repair and a previous failed enqueue retain the 250 ms gate, while a genuinely newer certificate following successful enqueue is eligible under the existing 50 ms urgent-finality rule. The map remains bounded by the immutable member roster and resets when the segment stops. Existing pending-demand deadlines, future/headroom limits and origin-owned completeness remain intact.

Independently reran the four new repair regressions: four passed, zero failed/skipped (`/private/tmp/behind-peer-independent-review.log`). They cover behind-finality completeness, selectively lost quiet pulses with working direct repair, simultaneous requester coalescing and role/scope/base rejection, plus exact fresh/duplicate/failed certificate timing. No concrete blocker remains in this small correction. Mobile-04's reported successful run preceded the final pacing refinement; qualification of the resulting source remains a separate parent-run measurement. No browser/build was run by this reviewer.


### Coordinator pause propagation — bounded design approval

Approve surfacing the existing authenticated kind-12 pause probe to the runtime so an active follower stops the old simulation while its coordinator prepares a lifecycle change. Validate the captured current association/channel/binding and authority before the callback. Runtime acceptance requires the installed coordinator, exact alias and current connection-scoped plan; unrelated peers, stale scopes and already-stopped segments cannot pause or extend waiting. This carries no new wire message and grants no state/authority change.

Record a first-pause marker before freeze callbacks for synchronous-delivery safety. Start one nonextendable five-second wait for a new plan and retain/start the existing fifteen-second recovery episode. A duplicate current plan does not satisfy that wait; only adoption of a new validated plan does. Preserve the episode through preparation until ordinary activation success, with the existing explicit no-coordinator lobby exception. Expected management waiting must not write `lastFault` or charge corrupt recovery; deadline expiry uses the existing visible recovery path. Stop input, action/control application and simulation immediately while leaving authorized plan/bootstrap processing available.

Recommend one guarded existing pause probe on the first local pause, followed by the existing 200 ms retries. Repeated freeze calls cannot create extra immediate probes. This reduces avoidable notice delay without assuming unreliable delivery will always precede the unchanged execution cap. Implementation regressions should cover delayed rematch plan beyond old-world headroom, successful new-scope activation, no-plan timeout despite duplicate pause/current-plan traffic, stale/foreign rejection and callback reentrancy. Resulting source review remains required.


### Coordinator pause implementation review — frozen-view correction required

The new transport callback is scoped to the current authenticated binding and rechecks association/channel/binding identity after runtime callbacks. Local deactivation emits only the first immediate pause probe. Runtime installs its wait marker before stopping the captured segment, accepts only the installed current coordinator/plan, keeps duplicate deadlines fixed and leaves a newer plan able to complete setup.

A remaining presentation/control-status path defeats a complete freeze: in `runtime.tick`, a stopped full simulator fails the healthy-world branch but falls through to the controller timestamp extrapolation branch, which currently checks only segment/view/clock readiness. It therefore keeps rewriting/publishing `view.tick` while its actual world is stopped. Restrict extrapolation to healthy controller-only segments, and prevent queued old-scope `directStatus` from changing a stopped controller's frozen view. Extend the delayed-plan regression to compare published and rendered snapshots, not only `replicaTick`. This is the remaining bounded pause-review blocker; no browser/build was run.


### Coordinator pause propagation implementation approval

The frozen-view finding is closed. Timestamp extrapolation now requires a healthy controller-only segment; stopped full simulators and stopped controllers cannot advance published ticks through that branch. Old-segment status additionally requires an activated healthy controller segment, so queued status cannot overwrite its paused scene. The delayed-plan test captures published and rendered snapshots separately at the pause and proves each remains fixed; this correctly avoids assuming they were identical before pause under existing callback coalescing.

Independently ran the three pause regressions: three passed, zero failed/skipped (`/private/tmp/direct-pause-independent-final.log`). They cover foreign/stale pause rejection, delayed-plan success without a spurious fault, unchanged published/rendered state during waiting, duplicate pause/current-plan traffic not extending the five-second deadline, bounded missing-plan recovery, and rejection of queued controller status. Together with the inspected transport binding/callback guards and first-pause probe behavior, no concrete blocker remains in this bounded implementation review. Approve coordinator pause propagation. Complete checks and current-source repeated native impairment qualification remain separate evidence; no browser/build was run by this reviewer.


### Retry transport activation before installing a segment — design approval

Approve the bounded activation correction: attempt all peer pulse activations before constructing/replacing `DirectSegment` or choosing the coordinator start instant. A transient failed readiness check retains the frozen old world and validated preparation candidate, emits no applied acknowledgement and retries through the existing source activation cadence. Partial local pulse flags are safe while no new segment can accept application pulses or produce current-alias proof; bootstrap probing continues and activation is idempotent. Do not undo irreversible pause flags or invent a new alias locally.

Preserve the original attempt/episode deadlines and coordinator applied guard. Recheck captured plan, preparation and prior-segment identity, current authority/connections and stopped/recovery state after external transport calls and before installation. Enforce existing timeout/start guards at callback admission or immediately before commit as well, so a retry arriving after the deadline but before the next scheduler tick cannot bypass it. Choose the start instant and publish/install exactly once only after the complete transport admission succeeds.

Required bounded regressions: transient activation loss after readiness converges in the same alias without partial world replacement/recovery; permanent unavailability follows existing bounded failure; synchronous authority/scope replacement aborts installation; partial pulse activation remains without application proof. This is design approval only. The sustained mobile failure remains recorded and current-source qualification is not inferred from a future unit fix.


### Transient activation readiness implementation approval

Approve the bounded activation change. Runtime attempts every required pulse activation before setting `startAt` or constructing/installing the replacement world. A failed link leaves the frozen previous segment and preparation candidate intact for the existing retry. The commit predicate checks captured plan/preparation/previous-segment identity, stopped/recovery/fault state, authority and current connections after each external transport call; it also enforces the original five-second attempt limit before any binding work or world installation. Existing start safety is checked on entry. Partial pulse activation remains idempotent and does not itself supply accepted application proof.

Independently ran all five new regressions: five passed, zero failed/skipped (`/private/tmp/direct-activation-independent-final.log`). They cover temporary failure on coordinator and follower with same-alias convergence and no partial publication, permanent failure through bounded recovery, synchronous authority replacement, and an expired callback arriving before the scheduler. No concrete blocker remains in this scoped review. The prior sustained mobile failure remains evidence; full checks and a current-source sustained run remain separate acceptance work. No browser/build was run by this reviewer.


### Reliable coordinator-pause reinforcement — design correction required

The sustained failure establishes why an immediate unreliable pause alone cannot reliably stop an old world before rebinding removes its retry path. Sending the same existing kind-12 tuple on the ordered game channel is a suitable bounded reinforcement, with current-alias/association receive validation and the same runtime coordinator restriction.

The initial proposal needs an explicit ordering barrier. If the first reliable enqueue fails, a later checkLinks retry is not necessarily ordered before intervening preparation sends. Retain one immutable pending pause per Link and flush it before subsequent lifecycle/gameplay-management sends on that channel; refuse those sends while it remains pending. Keep association bootstrap probes' existing escape path. Refuse new pulse activation while that link's pause is pending, preventing a second activated segment from replacing the one-entry notice. Preserve the pending old alias across binding changes; retire only on successful reliable enqueue or association/authority/connection invalidation.

Capture old alias/probe ID and incarnation/epoch/local/remote connections at authorized deactivation. Send with current channel/drain/visibility/authority/buffer guards but the existing probe-style health escape, since switching from pulse mode can itself make the shorter health window stale. Receive only the exact four-element tuple within 512 bytes and share existing total/probe ingress budgets across lanes. Receipt itself does not establish health. Fast/reliable duplicate copies remain idempotent and cannot extend runtime deadlines. Test failed first enqueue followed by rebinding/preparation and assert actual FIFO send order, one-slot preservation, activation gating, scope invalidation and unchanged failure deadlines. No implementation approval yet; record the ordering clarification before source changes.


### Reliable pause reinforcement design approval

The parent accepted the ordering correction: flush the immutable pending pause before `sendEnvelope`, `sendBound` and new `activatePulse`; block each while enqueue remains unavailable. `checkLinks` retries before normal bootstrap probes, which retain their escape path. New alias bindings retain the captured old-alias pause, while link/authority/connection replacement invalidates it. The receiver shares exact-tuple, alias, association and existing ingress budgets with fast pause handling. This satisfies the single-entry resource bound and ordered-before-preparation contract. Approved for implementation.

Install the pending item before external sends and clear it after success only if it remains the same item on the same Link; reentrant callbacks cannot clear replacement state. The planned native regression drops fast delivery, blocks reliable buffering, deactivates alias 7 and binds alias 8, proves management/activation refusal, then drains and observes pause 7 before a marker at a receiver still bound to 7 in both browser engines. Implementation review remains separate.


### Reliable pause reinforcement implementation approval

Approve the bounded source implementation. The pending notice captures alias and authority/connection identities before the fast send, persists across newer alias binding, and is flushed before management, compact control and activation. Failed enqueue preserves the single entry and blocks those operations; bootstrap probes retain their existing independent path. Successful enqueue clears only the same pending item on the same Link, while retired associations/scopes cannot flush it. The scope naming mirrors the existing binding (`sender` is the remote connection and `receiver` the local one), and checks use that convention consistently.

Reliable ingress validates the exact four-field pause schema, shares total/probe budgets with the fast lane, requires current authority/association/alias and undrained gates, then uses the same irreversible pause/runtime callback. It performs no subsequent work after that callback; fast ingress retains its post-callback link/binding checks before deferring a response. No received pause manufactures health evidence or resets runtime deadlines.

Independently ran the compact-control/schema suite: two passed, zero failed/skipped (`/private/tmp/direct-reliable-pause-independent.log`). Inspected the parent's active native-run log containing a passed Chrome/WebKit phase for dropped fast copies, blocked reliable enqueue, alias rebinding, ordered pause-before-marker delivery and ignored stale replay. This records that phase only, not an unobserved whole-process exit. No concrete blocker remains in the bounded implementation review. Full native completion and a new sustained impairment run remain separate qualification evidence; no browser/build was run by this reviewer.


Native completion follow-up: the parent confirmed exit 0 for `/private/tmp/direct-reliable-pause-browser-final.log` with no browser/teardown errors; inspected its completed phase output. The stale replay assertion now waits for a distinct `afterReplay` marker on the same ordered channel, so a prior marker cannot satisfy the barrier. Transport source is unchanged from the scoped approval. This strengthens native boundary evidence without replacing the still-required sustained qualification.
