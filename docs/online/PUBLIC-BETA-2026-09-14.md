# Public online beta — 2026-09-14

[Play Fuse Riders](https://andeplane.github.io/fuse-riders/). The Pages frontend and Cloud Run room service are deployed. The host can create a room on a phone, add AI opponents, invite friends, choose shared TV or individual screens, and save game preferences locally.

The creator's browser runs the game and must remain in the foreground. Gameplay requires direct WebRTC; no TURN or backend gameplay relay is configured. Failed direct connections produce retry states. These are deliberate product limits, not guarantees that every network can connect.

## Final runtime release

Runtime source **6c1673b1ba7121d03b6184461c354d8f453ec33a** passed CI **34796940787** and Pages **34797212683**. Root independently fetched its public manifest and healthy backend. [Final public Chrome/WebKit evidence](PUBLIC-ACCEPTANCE.md#final-latency-candidate-6c1673b) verifies the actual deployed bundle with zero page errors or failed HTTP responses. Both browser runs completed phone hosting, AI, guest RTC/scoring, reset, saved settings and separate-TV controls. Subsequent documentation-only commits preserve this exact tested runtime identity. It remains the last runtime with a clean public acceptance record; the restored UI releases below replaced it as the served bundle.

Local final validation also passed 321 tests, both type checks, the production build and enforced coverage (99.47% lines across the configured coverage modules). The isolated local benchmark service and all automated test browsers were closed; the user’s LAN game was not restarted.

## Restored UI release: 68bea0d

Runtime source **`68bea0db91de41707ebd5119ec43e7c23826ff65`** was `main` at publication (superseded by `a4bee00` below). It merges PR #1 (`codex/room-stability`, merged as `e084d01`) and PR #2 (`codex/restore-game-ui`): the restored neon room lobby with live avatar cards and a persistent shared-TV QR lobby, short `AB42` room codes with host-session lifetime and explicit **END ROOM** ([ADR 039](../adr/039-short-lived-room-codes.md)), landscape phone gameplay with three full-height touch thirds, desktop keyboard steering and firing, separate powerup configuration, Phaser load cancellation before navigation, and the explicit locally verified deployment path in `scripts/deploy-cloud.sh` and `.github/workflows/pages.yml`.

Local verification reported by the operator for this exact revision: 360 tests passed, both type checks, the production build, and configured coverage of 99.51% lines / 94.12% branches / 99.36% functions. Chrome and WebKit local menu, landscape, keyboard, shared QR and room flows passed and their screenshots were inspected. The user explicitly allowed merge and deployment after these local checks without waiting for CI. The raw output of that final local run is not preserved in this repository; the in-repository browser evidence for the same features was captured at intermediate PR revisions and is linked from [public acceptance](PUBLIC-ACCEPTANCE.md#restored-ui-release-68bea0d-local-evidence-only).

Pages manual `workflow_dispatch` run **34819458582** (actor `andeplane`, head `68bea0d`) completed successfully at `2026-09-14T07:47:49Z`. Because it was dispatched with the workflow's `local_verification` input, the public `release.json` identifies source `68bea0d` with `sourceVerification.mode=local` and `verifiedCiRun=null`, as reported by the operator. **This is a locally verified deployment, not a CI-certified release.** CI push run **34819425266** for `68bea0d` completed **with failure** at `2026-09-14T07:56:11Z`, after the Pages publication: type checks, enforced coverage, build, Chrome/WebKit five-phone LAN smoke, online Canvas room smoke, Phaser lifecycle checks and the landing/solo/mobile-menu step all passed on the runner, then the "Desktop keyboard steering and firing" step failed with `AssertionError: ArrowRight must turn authoritative pose: -0.28` from `scripts/keyboard-smoke.ts`, and the landscape, shared-QR and benchmark steps were skipped. The runner's `verification-evidence` artifact was uploaded. The operator's local run of the same script passed in Chromium and WebKit. PR #9 (`4876f0c`) fixed the test side: the script measured the right turn three ticks after keydown, before the scheduled input had necessarily reached the authority, so the runner's sample still included the preceding left turn; it now waits for the authoritative held controls before measuring. The next main run 34820779282 (`edda45d`, PR #8) still failed the same step waiting for the RECHARGE label, which PR #11 (`a4bee00`) fixed as a product defect ([issue #10](https://github.com/andeplane/fuse-riders/issues/10): the first fresh bomb press after reset or idle could be ignored) together with stronger authoritative charge/cooldown checks. CI run **34822503284** for `a4bee00` then passed every step, including "Desktop keyboard steering and firing", so the `68bea0d` failure was a test-timing issue and the keyboard gate is green on `main`; `68bea0d` itself never received a green CI run. The earlier CI run 34818639398 for the PR #1 merge `e084d01` failed at the "Landing, offline solo and mobile menus" step, and PR #2 subsequently changed the workflow steps, so that result neither certifies nor condemns `68bea0d`.

What this release does not prove: no successful CI run for `68bea0d` itself, no clean public Chrome/WebKit acceptance run against the deployed restored-UI bundle, no physical-phone behavior ([issue #4](https://github.com/andeplane/fuse-riders/issues/4)) and no real Wi-Fi/WAN measurement ([issue #5](https://github.com/andeplane/fuse-riders/issues/5)). The operator's final handoff comment on [issue #6](https://github.com/andeplane/fuse-riders/issues/6) records the matching backend build `cf51cf8d-07ee-49cf-a86d-9690563ed1bc` as Cloud Run revision `fuse-riders-gateway-00004-wt6` at 100% traffic, a passing public backend HTTP/WSS smoke, passing public Chrome product checks, and a public WebKit run that completed every product-flow assertion but failed its `pageerror` gate on an `RTCDataChannel` send from `sendDirectProbe` ([issue #7](https://github.com/andeplane/fuse-riders/issues/7), fixed by PR #18 after `a4bee00`); the raw `artifacts/*.json` outputs remain operator-local, so the WebKit result must not be described as clean public acceptance. See [GCP inventory](GCP-INVENTORY.md#restored-ui-release-68bea0d).

## Current runtime: a4bee00

Runtime source **`a4bee0082b3ef6b0d04c8e509a4df442f463e4be`** merges PRs #8, #9 and #11 on top of `68bea0d` (issue-workflow docs, keyboard test synchronization, and the first-bomb-press fix for issue #10 with stronger keyboard CI checks). CI push run **34822503284** completed with success at `2026-09-14T08:35:18Z` with every browser step green, and the automatic Pages run **34823487982** (`workflow_run`) completed with success at `2026-09-14T08:36:16Z`. The public `release.json`, fetched on 2026-09-14, reports `gitRevision` `a4bee00…`, `sourceVerification.mode=ci`, `verifiedCiRun=34822503284` and the unchanged API origin. The backend sources (`service`, `games/fuse-riders/src/shared`, `Dockerfile`) are identical between `68bea0d` and `a4bee00`, so revision `00004-wt6` serves this frontend. No public Chrome/WebKit acceptance run at `a4bee00` is recorded; the local restored-UI evidence linked above applies to the same UI. Later `main` merges (#32, #36, #40, #38, #18) had their push CI runs cancelled by the workflow's `cancel-in-progress` concurrency group as newer commits arrived and their Pages runs skipped, and PR #40 restricted CI push runs to `main`; check `gh run list --branch main` before citing a newer served runtime. After more merges, the "Desktop keyboard steering and firing" step started timing out again under runner load ([issue #43](https://github.com/andeplane/fuse-riders/issues/43), open); PR #52 (`0b5183a`) made that CI step non-blocking (`continue-on-error: true`) so Pages deploys are not gated on it while #43 is investigated, and PR #57 (open, not yet merged as of 2026-09-14) proposes a root-cause fix and reverts that override, restoring the step to blocking.

## Release history

| Check                           | Exact evidence                                                                                                                                                                                                                                                                                                                                                          |
| ------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Deployed backend                | Source `66f67bd0fb1f751f0d2a8a9f2ec8b0a9bc4d32d5`, revision `fuse-riders-gateway-00003-qf7`, 100% traffic; [inventory](GCP-INVENTORY.md)                                                                                                                                                                                                                                |
| Public provider smoke           | [Passed against the deployed service](evidence/cloud-public-smoke-66f67bd-2026-09-14.json), using public HTTP/WSS endpoints without operator credentials                                                                                                                                                                                                                |
| Expanded public browser check   | Chrome and WebKit passed at frontend `d715642ebd4d0cc63c5e5639a0adee6c3f4ab05d`, CI `34794220106`, with zero page errors or failed HTTP responses; [raw results and scope](PUBLIC-ACCEPTANCE.md#final-expanded-public-check)                                                                                                                                            |
| Subsequent frontend publication | Source `e1dcc6f0f35f518d86c066a5f6c11ad622dd9330`, CI `34795238231` and Pages `34795480877` succeeded. This publication is distinct from the completed public browser check above.                                                                                                                                                                                      |
| Final CI-certified runtime      | Source `6c1673b1ba7121d03b6184461c354d8f453ec33a`, CI `34796940787`, Pages `34797212683`, clean public Chrome/WebKit check; [raw result](public-acceptance-6c1673b.json)                                                                                                                                                                                                |
| Restored UI release             | Source `68bea0db91de41707ebd5119ec43e7c23826ff65`, manual Pages `34819458582` with `sourceVerification.mode=local` and `verifiedCiRun=null`; local checks only; CI `34819425266` later failed at the desktop keyboard browser step (fixed by PRs #9/#11); operator-reported public Chrome pass and WebKit `pageerror` failure on issue #6, no clean public run recorded |
| Current CI-certified runtime    | Source `a4bee0082b3ef6b0d04c8e509a4df442f463e4be`, CI `34822503284`, Pages `34823487982`, `release.json` `sourceVerification.mode=ci`; same backend revision `00004-wt6`; no public browser run recorded                                                                                                                                                                |

Backend origin: `https://fuse-riders-gateway-oaaqztec5a-ew.a.run.app`. Runtime identity: `fuse-riders-runtime@andershaf-87.iam.gserviceaccount.com`. Exact image, provider resources, account scopes, operating limits and rollback instructions are in [GCP inventory](GCP-INVENTORY.md) and [deployment instructions](GCP-DEPLOY.md). Later frontend-only commits do not imply a new backend image.

The expanded public check exercised a phone-sized host creating and joining a room, adding AI, accepting a guest over WebRTC, starting a scored round and resetting. It also verified shared-screen mode, saved length/powerup preferences across refresh, and a separate TV started/reset from the phone. These are automated desktop Chrome/WebKit checks, not physical-phone measurements.

## Performance evidence and remaining work

- [AI riders](AI-RIDERS.md): ordinary input/physics, five combined human/AI slots, mid-race additions join next round; deterministic tests and browser scoring checks passed.
- [Phaser benchmarks](../PHASER.md): desktop and mobile-sized views measured with production backing resolution. Mobile-sized frame p95 was 16.7 ms in Chrome and 18 ms in WebKit; physical hardware remains unverified.
- [Network evidence](NETWORK-EVIDENCE-2026-09-14.md): a 30-minute soak passed its documented scope at source `66f67bd`. Later 20 Hz regional and poor profiles passed recovery/freshness checks, but poor-network correction tails remain large. Application-message impairment is not OS-level packet loss.
- [Actual response benchmark](RESPONSE-BENCHMARK.md): the reviewed nearby-buffer candidate passes the predeclared 180-second run and combined 240-second batch: local p95 27.6 ms, TV p95 88.2 ms. Earlier failures remain in the report. The longer run observed 3.41% repeated TV ticks, with hold p95 34 ms and maximum 51.2 ms. The final 6c1673b candidate subsequently passed CI, Pages deployment and both public browser checks.

Physical touch-to-photon, background/lock and network-transition behavior are not established by phone-sized browser tests. Each linked report identifies its tested artifact; newer commits and running CI are not evidence of a completed test.

## Preserved deployment history

The initial backend `00001-5mm` smoke failed at Google's reserved `/healthz` route. Revision `00002-x2q` introduced `/api/health` and `/api/ready`; revision `00003-qf7` has the matching-source public proof above. Earlier manifests and failures remain in [deployment history](GCP-INVENTORY.md#initial-deployment).

Initial public frontend `2ff8843` completed the game flow in WebKit but logged an RTC send error. The [initial raw report](evidence/public-browser-initial-2ff8843-2026-09-14.json) retains that failure; the separate clean `d715642` retest resolves the observed symptom for the tested sequence without claiming that all possible RTC failures are impossible.

The public provider smoke verifies health, CORS, room creation, host/guest signalling, ICE metadata, rejection of gameplay relay, lease renewal and fenced host replacement. It does not force two Cloud Run instances or establish real WebRTC by itself; the browser checks establish the latter. Temporary test sockets were closed and disposable records are left to Firestore TTL. No occupied user room was used. Refresh all clients together when releasing protocol changes.
