# Initial public acceptance — 2026-09-14

This is positive-path public deployment evidence, not poor-network or physical-phone qualification. Two fresh disposable rooms were created through the actual Pages UI; no occupied LAN game was used. Chrome and WebKit ran sequentially with a desktop host and mobile-sized guest in separate browser contexts.

- Frontend: `https://andeplane.github.io/fuse-riders/`
- Served release: `2ff884388cfd0930088bade0bc849bc936d46bb7`
- Served CI identity: `34791214391`
- Configured backend: `https://fuse-riders-gateway-oaaqztec5a-ew.a.run.app`
- Raw results: [public-initial-acceptance.json](public-initial-acceptance.json).
- Reproduction: `node scripts/public-release-smoke.mjs` (creates fresh public test rooms).

Both browsers completed UI room creation, host join, ADD AI, guest join over direct WebRTC, received countdown, Phaser WebGL rendering and a normally scored round. Both reported one direct peer and zero relayed peers. No failed HTTP asset responses were recorded. Chrome recorded no page errors. WebKit recorded **one `Error sending string through RTCDataChannel.` page error**, despite completing those functional assertions. The original harness recorded the message only, so its stack and exact originating send path remain unknown; the checked-in runner captures stacks in future runs. Do not call this a clean WebKit release until the deployed fix is rerun.

The raw `pass` fields refer to the completed functional assertions; they do not override the separately recorded WebKit error. Snapshot metrics are individual samples, not latency distributions. Both environments were automated desktop browsers; mobile viewport emulation does not establish physical-phone compatibility.

[Actual public Phaser screenshot](../public-phaser-chrome.png) shows the countdown with the host, AI and guest visible. It is not an image-generated mockup or a screenshot of the later scored round. The hosted release predates later keyframe-recovery and shot-failure changes and cannot certify those changes.

## Final expanded public check

The expanded check subsequently passed **both Chrome and WebKit with zero page errors and zero failed HTTP responses** against served frontend `d715642ebd4d0cc63c5e5639a0adee6c3f4ab05d`, verified CI `34794220106`, built `2026-09-14T01:00:44.513Z`, and the same configured GCP backend. [Final raw report](public-final-acceptance.json) preserves the actual served manifest and results separately from the initial check above.

For each browser, a 390×844 host created a room, joined, added AI, accepted a direct WebRTC guest, started a normally scored round and reset to the lobby. The host then selected shared-screen mode, length 2 and blast weight 0; both phone boards hid. After host refresh, the settings dialog and saved preferences retained those values in the restored lobby. A separate TV showed Phaser while hiding host-only controls, and the phone host started and reset its race. The guest context was closed before opening the TV to keep at most two live views on the runner. These checks distinguish saved preferences from changes to an already-running match.

[Public phone host](../public-phone-host-chrome.png) and [separate public TV](../public-shared-tv-webkit.png) are actual browser screenshots. This clean retest resolves the earlier observed WebKit page-error symptom for this sequence; it does not prove the absence of all possible RTC failures. Physical phones, arbitrary NATs and poor-network performance are outside this positive-path public check.

## Final latency candidate: 6c1673b

After the later latency changes, CI **34796940787** and Pages run **34797212683** both completed successfully for exact source **`6c1673b1ba7121d03b6184461c354d8f453ec33a`**. The public `release.json` confirmed that source, CI identity, base `/fuse-riders/`, build timestamp `2026-09-14T01:51:00.799Z`, and the same verified GCP API origin before the browser run.

The expanded public script passed again in **Chrome and WebKit, with zero page errors and zero failed HTTP responses**. Every assertion described above passed: 390×844 phone host creation/join/start/reset, AI addition and scoring, direct RTC guest, shared phone boards hidden, length 2 and blast weight 0 retained after refresh, separate non-host Phaser TV and phone control of TV start/reset. All test browser contexts closed normally afterward. [Candidate-specific raw result](public-acceptance-6c1673b.json), [phone-host screenshot](../public-phone-host-6c1673b.png), [TV screenshot](../public-shared-tv-6c1673b.png).

The earlier `d715642` result and screenshot files remain unchanged. These final public checks establish product behavior on the actual deployed latency candidate; their individual FPS/RTT samples are not a replacement for the separately recorded response, impairment or physical-device qualification.

## Restored UI release: 68bea0d (local evidence only)

Runtime source **`68bea0db91de41707ebd5119ec43e7c23826ff65`** (merged PRs #1/#2) is served by manual Pages run **34819458582** with `sourceVerification.mode=local` and `verifiedCiRun=null`. **No clean public Chrome/WebKit acceptance run against the deployed `68bea0d` bundle is recorded in this repository.** `scripts/public-release-smoke.mjs` was updated in PR #2 for the restored lobby and landscape menu flows. The operator's final handoff comment on [issue #6](https://github.com/andeplane/fuse-riders/issues/6) reports one run of it against the public deployment: Chrome passed with no page errors or failed requests, and WebKit completed every product-flow assertion but failed the `pageerror` gate on an `RTCDataChannel` send from `sendDirectProbe` ([issue #7](https://github.com/andeplane/fuse-riders/issues/7), fixed by PR #18); its raw `artifacts/public-acceptance.json` is operator-local and not checked in. The `6c1673b` result above therefore remains the last clean public acceptance record, and it predates the restored UI. `68bea0d` was superseded as the served bundle by the CI-certified `a4bee00` (CI 34822503284, Pages 34823487982), which has no public run either.

The evidence that does exist for the restored UI was captured locally, in Chrome and WebKit desktop browser contexts, at intermediate revisions of the PR branches rather than at `68bea0d` itself:

- [Controller and neon lobby acceptance](ui-evidence/README.md) at source `1a25594`: shared QR/code, phone joining, separate TV, start/reset/end and authoritative steering; [desktop lobby](ui-evidence/lobby-desktop-1a25594.png), [WebKit portrait controller](ui-evidence/controller-portrait-webkit-1a25594.png), [WebKit landscape controller](ui-evidence/controller-landscape-webkit-1a25594.png), [raw result](ui-evidence/controller-lobby-1a25594.json) and the retained [initial WebKit navigation failure](ui-evidence/controller-initial-navigation-failure.json).
- [Landscape phone gameplay](mobile-landscape-evidence/README.md) at base `86183b5` with an uncommitted overlay ([build identity](mobile-landscape-evidence/build.json)): exact touch thirds, contain sizing, faded hints and rotation cancellation; [shared landscape thirds](mobile-landscape-evidence/shared-landscape-thirds-webkit.png), [faded solo overlay](mobile-landscape-evidence/mobile-landscape-faded-webkit.png), [portrait tools](mobile-landscape-evidence/mobile-portrait-tools-webkit.png), [passing result](mobile-landscape-evidence/chrome-webkit-passing.json) and the retained [initial WebKit touch-detection failure](mobile-landscape-evidence/initial-webkit-touch-detection-failure.json).
- [Desktop keyboard acceptance](keyboard-evidence/README.md) at source `1d58300`: arrow steering, Space charge/release, modal cancellation and editable inputs in Chromium and WebKit; [raw result](keyboard-evidence/desktop-chrome-webkit.json). The same script failed on the CI runner for `68bea0d` (run 34819425266, `ArrowRight must turn authoritative pose: -0.28`), so this local pass does not stand in for a green CI step; PRs #9 and #11 later fixed the step and CI 34822503284 at `a4bee00` passed it.
- [Landing, solo and mobile dialog acceptance](HOME-MOBILE-ACCEPTANCE.md) at implementation commit `4ff9427` plus the shared QR lobby follow-up: [390 px Chrome landing](home-evidence/landing-390-chrome.png), [shared QR lobby](home-evidence/shared-qr-webkit.png), [passing matrix](home-evidence/passing-matrix-avatar-bounds.json) and [short-room smoke](home-evidence/shared-room-smoke.json).

The operator additionally reported a final local run at `68bea0d` (Chrome and WebKit menu, landscape, keyboard, shared QR and room flows with inspected screenshots, alongside 360 passing tests, both type checks, the build and configured coverage of 99.51% lines / 94.12% branches / 99.36% functions); its raw output is not in this repository. All of this is desktop browser emulation against isolated local services. It does not establish the public deployment, physical phones ([issue #4](https://github.com/andeplane/fuse-riders/issues/4)) or real network behavior ([issue #5](https://github.com/andeplane/fuse-riders/issues/5)). A clean public rerun of the expanded check at the current runtime (`a4bee00` or later) should be recorded here before the restored UI is described as publicly accepted.
