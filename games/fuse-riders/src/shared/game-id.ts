/**
 * Fuse Riders' gameId on the shared backend: its rooms, match records, ratings and routes. It is the legacy game
 * (`LEGACY_GAME_ID` in the network and platform packages): what an absent gameId means, so it must not change.
 */
export const GAME_ID = "fuse-riders";
