{
  "name": "dice",
  "version": "0.1.0",
  "private": true,
  "description": "Pig, the template game: roll a d6 or hold, first to 50 wins the round and two rounds win the match. Its rules run on fuse-netcode, its page (src/app/) on fuse-ui and fuse-network-fe.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "exports": {
    ".": "./src/game/index.ts",
    "./platform": "./src/platform.ts"
  },
  "dependencies": {
    "fuse-netcode": "workspace:*",
    "fuse-platform": "workspace:*"
  },
  "devDependencies": {
    "@fontsource/press-start-2p": "^5.3.0",
    "fuse-network-fe": "workspace:*",
    "fuse-ui": "workspace:*",
    "qrcode": "^1.5.4"
  }
}
